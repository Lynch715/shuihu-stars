# 水浒群星录 V10 重构规范 · 施工流程

状态：**施工中** · 阶段 0、1 已完成
基线：`水浒群星录_V9.9.114_稳定版(2).html`（11448 行 / 859KB）
已定方向：压平重构 · 稳步推图Boss卡一下 · 逐回合播放可快进 · 旧存档弃用 · 山水古朴美术风格 · 立绘内嵌守单文件

---

## 一、现状账本

11448 行 = 约 1900 行原始引擎 + 约 7000 行补丁层（30+ 块，V6.5 → V9.9.114）+ 336KB 内嵌数据。

**真正在跑的只有这些：**

| 系统 | 实际生效的实现 | 位置 |
|---|---|---|
| 战斗引擎 | V6.5 块 | 4794 calcDmg / 4807 getSkillTargets / 4859 executeSkill / 5011 runBattleStep |
| 结算发奖 | v65OnBattleEnd（IIFE 局部函数，没挂 window） | 5827 |
| 结算渲染 | onBattleEndFinal | 9997 |
| 敌方生成 | V9.9.114 enemyFromHero，从零构造，旁路前面 10 层 | 11132 / 11380 |
| 我方属性 | V9.9.114 patchClone | 10929 |
| 血量 | V9.9.114 reHp | 11265 |
| 渲染锁 | V9.9.6 | 10521 |

**死代码约 40%：**

- 7 个 `onBattleEnd` 包装 + 2 个 `endBattle` 包装全部不可达（真正出口是 `v65OnBattleEnd`，它从没挂到 window 上）
- V9.2.0 整块 346 行空转：把 `<script id="gameData">` 这个 DOM 元素当数据对象用，`GD.heroes` 恒为 undefined
- V9.1.4 整块无效：包装的 `window.calcUnit` 全文件不存在
- V8.9.12 整块无效：写进了 DOM 节点
- 图鉴写了两份，第二份 139 行被 `__COLLECTION_LOADED` 挡住
- V8.8.3 的 cloneHero 增强体：第 6647 行一句 `return obj;` 让后面 60 行永不可达（"擅长武器+20%"在战斗里从未生效过，只在面板上生效——这就是面板数值和实战数值对不上的原因）

**确认的玩法故障：**

1. **第 1 关必输** — 11424 行从 `Object.keys(HD)` 头部顺序补位，补进林冲/鲁智深/吴用/宋江/武松/李逵 6 名满配主角。V6.5 为第 1 章准备的 `rf=0.6` 削弱在 11438 被丢弃两次。
2. **战力对比是假的** — `calcStagePower` 只数 `st.enemies` 的长度（3），不含品质乘数、不知道会补到 9 人、不知道 makeEnemy 的 11 层包装。显示"稳操胜券"，实战 2 人 1265 血打 9 人 10304 血。
3. **羁绊 0 条触发** — 118 条 bond 的 `min_count` 逐条等于 `members.length`，"忠义堂聚义"要 8 人同时在阵。控制台还打印"羁绊 118 条"。
4. **专属武器和羁绊加成被平方** — `patchClone` 装了两层（HP 包装没继承 `__v9109` 标记），`exc` 和羁绊段没有幂等保护，实际是 `(1+x)²`。
5. **吃药永久抬高体力基底** — 9857 行 `target.base.hp = Math.max(base.hp, target.hp)`，而 `target.hp` 已是 `base.hp×5` 量级。写进存档，不可逆。
6. **奖励面板永远显示"经验 +50"** — `b.expGain` 从未赋值，走的是 `buildRewardHTML` 的写死兜底。真实发放是 50~150。
7. **敌方会跟着玩家变强** — V8.9.9 的读取顺序把 `G.heroes` 排在 `HD` 前面，玩家练到 Lv50 的林冲当敌人出现时，直接套用玩家那份 base。
8. **buff / steal 无效** — 322 条技能（占全库 26%）写入 `status.buff_*` 后全文件无人读取，只刷日志。
9. **技能永不轮换** — 5089 行恒取 `atkS[0]`，四技能角色终身只放第一个。
10. **CD 少一回合** — `sk.cur = sk.cd` 之后同一循环末尾立刻 `--`，`cd:1` 等于 `cd:0`。
11. **每 2 秒全量重绘** — 9955 行 `setInterval(fullLock, 2000)` → `render()`，这是界面跳动和卡顿的主源。
12. **刷新页面银两重置为 500** — V8.9.13 的 `load` 监听无条件写 `G.res.silver = 500`。
13. **战利品显示但不入账** — 5877 行的 `st.reward` 只用于显示，从不真正加钱。

**数据层污染：**

- 286 个角色里 **81 个 ID 是 `__` / `___2` / `____4`**，376 个技能 ID 是 `___s1`。名字、属性、bio 都是好的，只有 ID 被某次处理洗掉
- 45 个角色无 bio；`fang_jie` 挂了不存在的技能 `_atk_fang_jie`
- 10 个关卡的 `enemies` 会被 V8.8.9 填成 `[undefined × 7]`（`_tpl[-1]` 取到 undefined）
- 26 个关卡被填成同一个 Boss × 7
- `enemy_templates` 是空对象 `{}`，所有相关代码是死的
- 标题写 V9.9.10，文件名 V9.9.114

---

## 二、目标架构

保持单文件 HTML。核心原则：**每个函数只有一份实现，不再有包装层。**

```
水浒群星录_V10.0.html
├─ <style>                     一份 CSS（现在三套战场网格样式并存）
├─ <script id="gameData">      清洗后的 JSON（见第三节）
└─ <script>                    一份引擎，分层：
     0  CONFIG      所有数值常量集中在这里，不散落在函数里
     1  DB          只读数据访问：DB.hero(id) / DB.skill(id) / DB.equip(id) / DB.stage(id) / DB.bonds()
     2  Save        单一 SAVE_KEY，全字段持久化，版本号 + 迁移钩子
     3  G           唯一全局状态
     4  Stats       ★ 统一属性口径，唯一入口 calcStats()
     5  Battle      createBattle / runRound / executeSkill / 状态机
     6  Grow        升级 / 升星 / 装备 / 羁绊 / 招募 / 掉落
     7  View        视图注册表 + 事件委托（零内联 onclick）
     8  boot()
```

禁止事项（写进代码注释）：

- 禁止 `window.X = function` 覆盖已有函数
- 禁止 `var orig = X; X = function(){...orig...}` 包装
- 禁止在渲染函数里写业务逻辑
- 禁止用 `skill.name.indexOf('盾')` 这类中文子串判定分支（一律按 `skill.type`）
- 禁止内联 `onclick`
- 禁止 `setInterval` 做数据兜底

---

## 三、数据层规范

### 3.1 ID 重建

81 个乱码角色 ID、376 个乱码技能 ID 按人名重建拼音 ID（`刘唐 → liu_tang`，技能 `liu_tang_s1`）。**必须同步更新所有引用点**：

- `stages[*].enemies`
- `stages[*].drops[*].id`、`stages[*].first_reward.hero`
- `bonds[*].members`
- `LEGEND_POOL` / `GOLD_POOL` / `EXC_POOL` / `EXCLUSIVE_POOL`
- `equipment[*].exclusive`

重建后跑一次全库引用校验：任何指向不存在 ID 的引用必须归零。

### 3.2 字段定型

| 字段 | 定型为 | 说明 |
|---|---|---|
| `hero.q` | 数字 1–6 | 不再有文字/数字两套口径来回转 |
| `hero.rarity` | 删除 | 由 q 派生，不存冗余字段 |
| `equipment.slot` | `weapon / armor / helm / mount / special` | 现在 `helm`(12) 和 `helmet`(2) 并存，代码只认 `helmet` |
| `stage.ch` | 补齐 | 现在靠运行时 `s.ch = s.chapter` 兜底 |
| `stage.enemy_lv` / `enemy_star` | 新增，显式写死 | 不再从 `rec_lv` 推导 |

### 3.3 补缺

- 45 个角色补 bio（从《水浒传》《荡寇志》《说岳全传》原著背景写，每人 60–100 字）
- `fang_jie` 的断链技能修掉
- 10 个空 `enemies` 关卡补真实敌方配置
- 26 个"同一 Boss × 7"的关卡重排

### 3.4 羁绊门槛分档

现在 118 条全是"全员到齐"，实测 0 条触发。改三档：

| 档位 | 条件 | 效果 |
|---|---|---|
| 初识 | 任意 2 名成员在阵 | 40% |
| 相契 | ≥ ceil(n/2) 名在阵 | 70% |
| 齐聚 | 全员在阵 | 100% |

成员只有 2 人的 bond 只有"齐聚"一档。羁绊面板要显示当前档位和差几人进下一档。

---

## 四、数值规范

### 4.1 统一属性口径（本次重构的核心）

**唯一入口 `calcStats(hid)`，纯函数，一次算完，无包装无幂等问题。**

```
S = base[k]                              // 含升级累积
  × QMULT[q]                             // 品质 1.0/1.1/1.2/1.35/1.5/1.7
  × STAR[star]                           // 星级 1.0/1.12/1.26/1.42/1.6
  × (1 + gr[k] × (lv-1))                 // 个人成长曲线
S = S + Σ装备flat[k]
S = S × (1 + cap(Σ装备pct[k], 0.30))
S = S × (1 + 擅长武器 0.20)               // 仅 atk，且武器 weapon_type == hero.fav_weapon
S = S × (1 + 专属武器加成)                // 仅 atk，专属武器绑定本人
S = S × (1 + cap(羁绊[k], 上限))          // atk/hp 0.25，其余 0.20
```

血量：`maxHp = calcStats(hid,'hp') × 5`。删掉所有 `lockHp` / `HP_LOCK` / `clampHp` / `sweep`，**血量只有这一个来源**。

**敌方走同一个 `calcStats`**，base 来自 HD 模板，lv/star 来自关卡的 `enemy_lv`/`enemy_star`，不含装备/羁绊。敌我同口径。

### 4.2 战力必须由 calcStats 派生

```
power(unit) = atk×2.0 + def×1.5 + int×1.2 + agi×1.0 + maxHp×0.05
```

`calcStagePower` 直接构造该关的敌方单位数组再求和，**不允许另写一套估算公式**。这样战力对比和实战结果天然一致。

### 4.3 难度曲线（稳步推图，Boss 卡一下）

以"该关推荐等级 + 对应星级 + 该章节可获得的装备"为基准算出 `P_std`，然后：

| 关卡类型 | 敌方总战力 / P_std | 体感 |
|---|---|---|
| 普通关 | 0.80 – 0.90 | 不用练级直接过 |
| 精英关 / 支线 | 0.95 – 1.05 | 需要合理上阵 |
| Boss 关 | 1.15 – 1.30 | 练两级或换装备 |
| 隐藏关 | 1.5+ | 后期回头打 |

**关卡敌方人数按数据写死（3–9 人），不再运行时补位。** 这一条直接解决第 1 关塞主角。

施工方式：写一个离线脚本，按上表反推每关的 `enemy_lv` / `enemy_star` / 人数，固化进 gameData，再跑模拟战验证胜率。

### 4.4 模拟验证

写一个 headless 批量模拟脚本：按推荐配置跑每关 200 场，输出胜率表。验收标准：

- 普通关胜率 ≥ 90%
- 精英关 70–85%
- Boss 关 45–65%
- 无任何一关胜率为 0% 或 100%

---

## 四之二、美术规范（山水古朴）

样张见同目录 `美术风格样张.html`。现有风格（深褐底 + 金色发光 + emoji + 圆角）整体废弃。

### 色板

| 用途 | 色值 | 说明 |
|---|---|---|
| 宣纸底 | `#e9e2d0` | 主背景，叠 SVG feTurbulence 纸纹 opacity .12 |
| 纸底二级 | `#e2dac6` | 卡片、版框内底 |
| 墨 | `#2a251f` | 正文 |
| 次墨 | `#5c5347` | 次级文字 |
| 淡墨 | `#8a8073` | 说明文字 |
| 极淡墨 | `#b3a893` | 分隔线、边框 |
| 朱砂 | `#9c2b23` | 印章、伤害、危险、强调 |
| 花青 | `#3f5a6b` | 数值、我方血条、护盾 |
| 泥金 | `#a8802c` | 星级、绝世描边 |

品质色（宣纸底上不靠发光，靠印章 + 传统色）：

凡 素墨 `#8a8073` · 良 花青 `#4a6b7c` · 猛 竹青 `#55703f` · 名 赭石 `#a35a2a` · 天罡 紫檀 `#6b4570` · 绝世 朱砂 `#9c2b23` + 泥金内描边

### 字体

- 标题：`"Kaiti SC","STKaiti","KaiTi","楷体",serif`
- 正文：`"Songti SC","STSong","SimSun","宋体",serif`
- 安卓无楷体时退回 serif。书名、章节名这类关键大字改用 SVG 勾边，保证任何机器一致
- 不嵌入字体文件（中文字体子集化后仍有 1–3MB）

### 组件

| 组件 | 做法 |
|---|---|
| 版框 | 古籍双边：`border 1px` + `inset box-shadow` 做内细外粗，不用圆角不用阴影 |
| 卷首 | 上下两条卷轴杆 + 竖排书名 + 朱砂方印 |
| 导航 | 书签式方标签，选中态填墨反白 |
| 章节标题 | 鱼尾（倒三角）+ 标题 + 渐隐横线 |
| 按钮 | 淡墨描边矩形，主按钮填墨反白，危险用朱砂描边 |
| 品质 | 右上角方印，篆意单字：凡／良／猛／名／罡／絕 |
| 星级 | 实心圆点 `● ○`，泥金色，不用 ★ |
| 战阵 | 3×3 淡墨方格，血条 2.5px 细线（我方花青／敌方朱砂），阵亡格盖朱砂「殁」 |
| 伤害飘字 | 楷体朱砂数字，纸色三重 text-shadow 描边压住底 |
| 图标 | emoji 全部废弃，换单色墨线 SVG 或直接用汉字（銀／金／將／盾／殁） |
| 远山 | 顶部 fixed SVG 剪影，opacity .055，向下渐隐遮罩 |

### 人物牌（立绘的兜底层）

代码生成，零体积，风格天然统一：

- 竖版卡，宣纸底，四角墨线装饰
- 右上品质方印
- 兵器剪影作主视觉：按 `fav_weapon` 自动选（枪／刀／剑／斧／棍／锤／鞭／戟／弓／禅杖／索／石），墨线 SVG，opacity .30，压在名字下层
- 姓名楷体大字 + 绰号小字
- 属性行 + 星级

### 立绘

- 数据加 `portrait` 字段：**有图显示立绘，无图回落人物牌**。可增量补，不阻塞重构
- 生产规范见同目录 `立绘生产规范.md`（固定风格块 + 八维差异化变量块 + 天罡36 完整 prompt）
- 出图由 Lynch 交 GPT 完成，我负责裁切／统一色调／压 WebP／生成 manifest／base64 内嵌
- 第一批：天罡36 + 主要 Boss 约 50 张，内嵌约 1.5MB，仍守住单文件
- 详情页 320×440，卡片 120×160，WebP q70
- 超过 80 张时重新衡量是否转外部文件夹 + PWA

### 立绘规格（首批 5 张实测后定稿）

**一个角色只出一张图**，三处显示全靠 CSS 缩放，整张 3:4 **不裁切**：

| 用处 | 显示尺寸 |
|---|---|
| 人物牌 | 满铺卡面，`object-fit:cover`，底部 56% 纸色渐变压住名字 |
| 角色详情 | 宽 150px |
| 战阵 | 88 / 60 / 40px，**按上阵人数自动切档** |

战阵切档规则（`data-n` 属性，纯 CSS）：1–3 人 88px、4–6 人 60px、7–9 人 40px；7 人以上收掉血量数字行（血条已足够，点格子看详细）。实测棋盘高度 458 / 658 / 657px，手机可视区 700px 内三档全部同屏。

原先设计的 112×112 头像裁切已废弃——裁切位置依赖构图，出图批次一变就切到头顶。

**处理流水线**（`build_portraits.py`，可复用）：

1. 各图绢底按四角采样，拉齐到全组平均，限幅 ±12%（首批实测蓝通道漂了 19%）
2. 整体向纸色 `#e9e2d0` 提亮 35%，与 UI 同色系，同时保留绢本旧感
3. 0.5px 高斯降噪，压掉绢纹高频噪点——体积省约三成，线条与眉眼无损
4. 480×640，WebP q62，method 6

**实测体积**：单人均 28.2KB。天罡 36 张 0.99MB（base64 1.32MB）／首批 50 张 1.38MB（base64 1.83MB）／108 将全出 2.97MB（base64 3.95MB）。

### 战斗场景

按**场景类型**做，不按关卡做，约 14–16 张覆盖 139 关（山林／水泊芦苇／风雪／荒野林道／庄院／城池街市／酒楼客店／官衙法场／寺观／关隘城墙／塞外大漠／江南水乡／夜战火攻／皇城宫阙／山道险隘／江面水战）。关卡加 `scene` 字段挂载。

同一张场景三处复用，不透明度不同：**卷首横幅 62%／战前剧情 34%／战阵背后 18%**。

已用纯 SVG 实现四张（山林／水泊芦苇／风雪／城关），手绘路径 + `feTurbulence` 位移滤镜做笔锋边缘，四张加滤镜定义共约 6KB。真水墨图作为可选覆盖层（`scene_img` 字段），有图用图无图用 SVG。SVG 换 `feTurbulence` 的 `seed` 即可免费生出山形变体。

---

## 五、战斗规范

### 5.1 技能类型全集

按 `skill.type` 分派，覆盖数据里真实存在的 13 种，一个不漏：

| type | 条数 | 行为 |
|---|---|---|
| `atk_single` | 345 | 单体，同列优先取最低血 |
| `buff_all` | 280 | 全队增益 `stat` / `value` / `dur` |
| `shld_self` | 165 | 自身护盾 `maxHp × mult` |
| `atk_row` | 114 | 打敌方同一横排（**按目标实际 row，不写死行号**） |
| `atk_col` | 91 | 打敌方同一纵列 |
| `chaos` | 70 | 混乱，`chance` 生效，有抗性递增 |
| `steal` | 42 | 偷属性：目标 -val，自身 +val（**按 type 判定，不按名字**） |
| `shld_all` | 37 | 全队护盾 |
| `atk_all` | 34 | 全体 |
| `heal_all` | 33 | 全队治疗 |
| `heal_one` | 14 | 单体治疗，取最低血友军 |
| `shld_col` | 1 | 同列护盾（现在会变成打人） |
| `heal_self` | 1 | 自身治疗（现在会治到敌人身上） |

技能对象必须完整复制数据字段：`id / name / type / desc / cd / mult / stat / value / dur / chance / eff`。现在只复制了前 6 个 + eff，导致 280 条 buff 全部退化成同一个 `atk +10 / 2回合`。

### 5.2 必修的战斗逻辑

- `buff_*` 要被读取：`calcDmg` 取 `atk + status.buff_atk.val`，`def` 同理
- 护盾在**所有**伤害路径抵伤（现在混乱攻击不抵）
- 护盾有持续回合，不是永久
- 混乱有抗性递增：同一单位每次被混乱后抗性 +25%
- CD 在**回合末统一递减**，不是行动后立刻
- 技能选择**轮换**：可用技能按上次使用回合排序，取最久未用的
- `battleGetEnemies/Allies` 按施法者阵营返回（现在忽略 `u` 参数，敌方治疗会治玩家）
- 回合上限读 `CONFIG.maxRounds`，不硬编码

### 5.3 站位

固定 3×3。我方 row 0/1/2，敌方 row 0/1/2，各自独立坐标系。`atk_row` / `atk_col` 按目标方的实际坐标判定。

### 5.4 演出（逐回合播放 + 可快进）

- 棋盘每回合刷新：血条、护盾、状态图标
- 伤害飘字：数值从格子上浮，暴击标红加大
- 技能释放：技能名短暂显示在施法者格子上方
- 回合日志区同步滚动，配色区分（技能紫 / 伤害红 / 治疗绿 / 护盾蓝）
- 回合间隔 `CONFIG.roundDelay` 默认 600ms
- 右上角「»」快进：跳过动画直接出结果
- 阵亡：格子灰化 + 短暂抖动

---

## 六、养成与开局节奏

### 6.1 开局

**固定起手**：史进（少华山主角，对应第 1 章剧情）+ 朱武。不再随机。开局银两 500，无装备。

第 1 关必须能稳过。

### 6.2 成长曲线

| 项 | 规则 |
|---|---|
| 经验 | `expNeed(lv) = 1000 × 1.15^(lv-1)`，单关掉落 50–150，首通额外经验书 |
| 升级 | 银两 `20 × 1.1^(lv-1)`；**升级只累加 `base`，不再在 `cloneHero` 里二次计等级成长**（现在算两遍） |
| 升星 | 碎片 `{1→2:2, 2→3:4, 3→4:8, 4→5:16}`，每次升星解锁 1 个技能，5 星共 4 技能 |
| 碎片 | 重复角色转碎片，按品质 `{1:1,2:2,3:3,4:5,5:8,6:12}` |

### 6.3 招募

现在的 `rollHero` 是**逐英雄加权**，不是逐稀有度——池里"名"有 88 人 × 0.08 的总权重远大于"凡"23 人 × 0.45，公布的 45/28/15/8/3/1% 完全是假的。

改成两段式：先按公布概率抽稀有度，再在该稀有度内等概率抽人。UI 显示的概率就是真实概率。

`rollWithPity` 目前无保底，名字骗人。加真保底：十连必出 ≥「名」，累计 50 抽必出 ≥「天罡」。

### 6.4 经济

- `G.res` 是唯一真相源，`G.items` 不再镜像货币（现在双写）
- 战利品显示什么就发什么（现在 `st.reward` 只显示不入账）
- 删掉铜钱/文（三处补丁在删，删不干净）

---

## 七、渲染与存档

### 7.1 渲染

```js
const VIEWS = { main, heroes, stages, stage_detail, hero_detail,
                team, items, bonds, tavern, collection,
                battle, battle_result };
```

- `render()` 只做：刷资源栏 → 刷导航 → `VIEWS[CUR.view]()`
- 全部事件走 `data-action` 委托，零内联 `onclick`
- 删掉所有定时器兜底（2s fullLock / 1.5s 空白守卫 / 5s 黄金自检 / 1s 锁轮询）
- 结算面板由状态驱动（`CUR.view = 'battle_result'` 有对应渲染函数），不再直写 `#content.innerHTML`

### 7.2 存档

- 单一 `SAVE_KEY = 'shuihu_v10'`，旧 key 不读
- 持久化全字段：`heroes / items / frags / cleared / team / res / log / clearCount / version`
- 写入守卫：`G.heroes` 为空或 `team` 非数组时拒绝写
- 重开：清 key → 重新 `initGame()`，不再 `location.replace`

---

## 八、施工流程

每阶段产出一个可玩 HTML + 跑回归脚本，你验收后进下一阶段。

| 阶段 | 内容 | 产出 | 验收 |
|---|---|---|---|
| **0 基线** ✅ | 22 项 headless 回归检查 | `test/regress.js` + `test/baseline_V9.9.114.json` | 已完成，基线见第十节 |
| **1 数据清洗** ✅ | ID 重建 + 字段定型 + 补 bio + 补关卡 + 羁绊分档 | `data/gameData_v10.json` + 两份报告 | 已完成，见第十一节 |
| **2 引擎压平 + 换皮** | 按最终生效版本重写为单层引擎，删死代码；**CSS 直接按山水古朴重写，不做两遍**；人物牌组件落地 | `V10.0-alpha.html` | 回归脚本全绿，行数 < 5000，无重复函数定义，零 emoji，零内联 onclick |
| **3 数值重做** | 统一 `calcStats` + 战力派生 + 难度曲线 + 血量体系 | `V10.0-beta.html` + 胜率表 | 模拟胜率达标（第四节 4.4） |
| **4 战斗演出** | 逐回合播放 + 朱砂飘字 + 快进 + 阵亡「殁」 | `V10.0-rc1.html` | 手机端一场战斗流畅播完，快进可用 |
| **5 开局与养成** | 固定起手（史进 + 朱武）+ 招募概率重做 + 经济修正 | `V10.0-rc2.html` | 新手 30 分钟能推到第 3 章 |
| **6 立绘接入** | 接 Lynch 出的图：裁切／统一色调／压 WebP／内嵌／`portrait` 挂载 | `V10.0-rc3.html` | 有图显示立绘，无图回落人物牌，体积可控 |
| **7 收尾** | UI 细修 + 全量回归 + 版本号统一 | `水浒群星录_V10.0.html` | 全部验收项通过 |

阶段 6 不阻塞 1–5 和 7。立绘什么时候到什么时候接，一张一张加都行。

阶段 1、2 是基础，改动量最大但风险最低（有回归脚本兜底）。阶段 3 是玩法真正变好的地方。

---

## 九、需要你拍板的点

已定：美术风格山水古朴（样张已确认方向）／立绘内嵌守单文件／立绘 prompt 由我出、图由 GPT 生成。

待定：

1. **旧文件保留策略**：新文件另存 `V10.0`，旧的 `V9.9.114` 原样保留不动？
2. **角色数量**：286 个角色里有 40 个是后期补丁注入的（`src:"v912"` 那批）。全保留，还是只留原著 + 荡寇志 + 说岳？
3. **bio 补写**：45 个缺 bio 的角色，我按原著背景写；如果你希望某种特定语感（比如贴近原文文白），说一声。
4. **阶段 4 的演出**：飘字和动画用纯 CSS 还是加轻量 JS 缓动？纯 CSS 性能好，JS 控制精细。倾向纯 CSS。



---

## 十、阶段 0 基线（V9.9.114 实测）

`node test/regress.js <html>`，22 项检查。基线结果 **通过 8 · 存疑 2 · 失败 12**，存档在 `test/baseline_V9.9.114.json`。

后续每阶段跑同一套，对照此表确认修复且无回退。

### 失败 12 项

| 检查项 | 实测 |
|---|---|
| 页面加载无 JS 错误 | Babel 块抛 1 个 TypeError |
| 角色 ID 无乱码 | 286 个中 81 个为 `__` / `___2` / `____4` |
| 技能 ID 无乱码 / 无断链 | 376 个乱码 ID；`fang_jie:_atk_fang_jie` 断链 |
| 角色 bio 完整 | 286 个中 45 个缺 bio |
| 开局角色固定 | 两次开局分别是「潘金莲、方肥」和「顾大嫂、郭盛」 |
| 第 1 关敌方配置合理 | 9 名敌人混入林冲(武228/血1461)、鲁智深(血1734)、武松、李逵，总血 10304 |
| 战力对比与实战一致 | 显示敌方 587，实际强度 6407，**差 10.9 倍** |
| 第 1 关可通关 | **20 场胜 0** |
| buff 技能有实际效果 | 挂 `buff_atk +5000` 后三回合总伤 930→906（0.97×），约 280 条 buff + 42 条 steal 无效 |
| 存档读档往返一致 | `G.frags`（升星唯一货币）不进存档，刷新即清零 |
| 刷新后银两不被重置 | 存 8888，刷新后变回 500 |
| 无定时器强制重绘 | 7 个 setInterval：30000 / 100 / 200 / 2000 / 1500 / 5000 / 5000 ms |

### 通过 8 项（重构后不得回退）

战斗日志非空（2 回合 32 条）／技能会轮换（9 种）／护盾能抵伤／升级生效且扣银两／升星消耗碎片并解锁技能／装备穿戴影响战斗属性／开局银两 500 且队伍 ID 有效／羁绊在真实阵容下可触发

### 存疑 2 项

- **招募概率与公示一致** — 抽卡函数封在 V6.9 的 IIFE 闭包里没暴露，脚本够不着。压平后函数会挂到统一入口，届时可自动校验。
- **奖励面板数字真实** — 第 1 关必输，走不到胜利面板。等难度修好后此项才有意义。

### 补充发现

- 羁绊即便用「最常同框的 9 人」组队，118 条也只激活 **8 条**
- 升级后战斗属性确实上涨，但 `cloneHero` 与 `applyLevelGrowth` 各算一次等级成长，存在**双重计算**（见第四节）


---

## 十一、阶段 1 数据清洗结果

脚本：`data/clean_data.py`（结构层）→ `data/fill_content.py`（内容层），均可重跑。
产出：`data/gameData_v10.json`（422KB）／`data/idmap.json`（ID 映射表）／两份报告。

### 终检

| 项 | 结果 |
|---|---|
| 角色 | **266**（258 − 1 合并 + 9 新增） |
| 技能 | 1227，每人恰好 4 个 |
| 装备 | **216**（并入 53 件专属神兵） |
| 关卡 | 139，敌方人数 **3–9**（原先 24 关只有 1 人、12 关为空） |
| 羁绊 | 118，全部改三档 |
| 非法 ID | 角色 0 / 技能 0 |
| 悬空引用 | **0 处** |
| 缺 bio | **0** |

### 过程中发现的原始数据硬伤

1. **单廷珪／单廷圭是同一人存了两份**（异体字，圣水将军，地煞 44）。已合并，保留带座次的记录。
2. **运行时注入的角色用另一套血量量纲**：中位 660 对 gameData 的 144，差 4.6 倍。并入的 9 人全部按品质中位数换算。这是四大寇章节敌人离谱的根源之一。
3. **ch21_f1 / ch26_f1 引用了不存在的角色**（`____18`、`___48`），原始数据坏引用。
4. **`andaquan` 是安道全拼漏一个 o**，导致一件专属神兵绑不上人。已修。另有一件装备绑「项羽」——非水浒人物，绑定已清除。
5. **36 个支线关敌方配置形同虚设**：24 关只有关名点到的那一个人，12 关为空。全部按阵营补齐到 6–9 人。
6. **4 个关名是拉丁占位**（`liao·宝藏·隐藏` 之类），改为北国秘藏／晋王府秘藏／楚王宫秘藏／帮源洞秘藏。

### ID 重建规则

只重建真正坏的 95 个（下划线乱码、汉字 ID、`*_hero` 占位），**不做全量拼音重建**——库里有不少现有 ID 比 pypinyin 更对：`gongsun_sheng`（复姓）、`qin_hui`（桧读 huì）、`wan_qixie`（万俟 mò qí）、`shan_tinggui`（单作姓读 shàn）。

同音不同人的处理：被重建的加绰号后缀，已有的正确 ID 不动，避免改到已被引用的 ID。

| 保持 | 加后缀 |
|---|---|
| 张清（没羽箭）`zhang_qing` | 张青（菜园子）`zhang_qing_cai` |
| 周侗（说岳）`zhou_tong` | 周通（小霸王）`zhou_tong_xiao` |
| 李云（青眼虎）`li_yun` | 李衮 `li_gun`（原 ID `li_yun` 是错的，衮读 gǔn） |

引用改写用三层兜底：已重建映射 → 被丢弃的注入别名 → 按姓名拼音反查（含去下划线匹配）。

### bio 补写

15 篇全部写入。其中 **5 篇标 [确]**（包道乙、施全、吉青、厉天祐、韩延寿，原著有明确记载），**10 篇标 [推]**（方裕、方皭、庄儒、程万、田琼、耿文、田实、真祥麟、哈兰生、沙志仁）——这些人原著着墨极少，按身份与阵营写，**未编造具体战绩**。标记留在 `data/fill_content.py` 的 BIO 表里，随时可替换。

### 尚未写入的字段

`enemy_lv` / `enemy_star` 尚未写入任何关卡——这两个字段是难度曲线的载体，在**阶段 3** 由离线脚本反推后统一固化。

---

## 12　阶段 4：战斗演出（已完成）

### 12.1　先做普查，再决定画什么

动手前用 `probe_fx.js` 在 35 个关卡、210 场战斗里统计真实出现的状态，
结果决定了画什么、不画什么：

| 状态 | 单位·回合出现次数 | 处理 |
|---|---|---|
| `buff_atk` | 38381 | 画（▲/▼ 小印） |
| `buff_def` | 17429 | 画 |
| `buff_int` / `agi` / `cha` | 2761 / 1089 / 531 | 画 |
| `stun` 眩晕 | 509 | 画，永远显示 |
| `chaos` 混乱 | 169 | 画，永远显示 |
| `bleed` 流血 | 19 | 画 |
| `burn` 灼烧 / `poison` 中毒 | 0 | 画（技能存在，只是罕见） |
| 护盾 | 几乎人人有 | **不画印记**——人人都有的标记等于没信息，改画在血条上 |

### 12.2　格子上的信息

- **状态小印**：右缘竖排，朱砂＝控制（眩/乱），赭石＝持续伤害（血/灼/毒），
  金＝增益，灰＝被夺。九人阵空间不够时增减益让位，控制类永远保留。
- **护盾画在血条上**：血条加高到 4px，护盾是压在上半层的一道金线，厚薄可见。
- **伤害数字过万折成「万」**：40 像素宽的格子上放不下五位数。

### 12.3　逐件演出，而不是一次泼完

引擎里一个回合是一瞬间算完的。原来把这一瞬间的结果一次性画上去，
九个人同时镶金边、九个数字同时往上飘，等于什么都没演。

现在 `Play.paint` 做三件事：

1. 倒着走一遍事件表，算出每一件事发生**之后**该显示多少血；
2. 把血条拨回本回合开打前；
3. 按 58–130ms 的间隔，一件一件放：出手者镶金边前倾，被打者红边抖动，
   暴击加墨渍滤镜，阵亡淡出盖「殁」印，血条跟着那一件事掉。

每回合发一个令牌，按下快进后上一回合排队中的定时器一律作废——
否则旧血量会把已经推进几回合的战况倒着写回去。

### 12.4　快进不再冻屏

原来 `paint()` 在 `UI.fast` 时直接 return，间隔设 0，
浏览器根本拿不到渲染的机会，整场战斗屏幕全程静止。
现在快进走轻量路径（只刷血条、状态、战报），间隔 60ms，
实测每 120ms 推进约 2 回合，画面每帧都在变。

### 12.5　顺手补的引擎漏洞

| 问题 | 影响面 | 处理 |
|---|---|---|
| `poison` 效果引擎两处都不认 | 3 个技能完全失效 | `applyEff` 与 `tick` 补上 |
| `buff_all` 的 `hp` 分支 | 4 个技能白写（maxHp 不经 `eff()` 读） | 改成当场回血 |
| 魅力在战斗里毫无作用 | 17 个只动魅的技能等于没写 | 魅＝气势，提升暴击率（`critOf`） |
| `buff_all` 小盖大 | 后手弱 buff 会覆盖前手强 buff | 取较大值 |
| 三十回合不下一律判负 | 约 10% 的战斗 | 改为以双方剩余血量比例论高下，结算页写明 |

### 12.6　难度曲线：从断崖到斜坡

演出做完后发现一个更根子的问题——**胜率对强度是一道断崖**：

```
ch1_boss   ×0.3:1.00  ×0.35:1.00  ×0.4:0.97  ×0.45:0.65  ×0.5:0.17  ×0.6:0.00
```

九对九打二十回合，大数定律把每一场抹成同一场，
Boss 只有「碾压」和「被碾压」两种样子，没有「险胜」。

两条措施：

1. **引擎加抖动**：单次伤害 ±16%，出手顺序 ±12%，集火 62% 打最虚的、
   其余随机——让战局有分叉。
2. **调参器改括号二分**：原来按「离目标最近」挑试探点，
   小样本下「谁大谁小」全是噪声。现在始终维持一个 `[偏易, 偏难]` 的括号，
   每次取中点收缩，大样本定稿，再在同一括号里收两次。
   另加抬级回退：系数要超过 5 倍才够，说明这关的底子撑不住，改抬敌方等级。

结果：

| | 阶段 3 收尾 | 阶段 4 收尾 |
|---|---|---|
| 落进目标区间 | 132/139 | **138/139** |
| 零胜率 / 全胜关 | 7 关 | **0 关** |
| 独立抽样复核 | — | **47/47** |
| 回归 | 22/22 | 22/22 |
| 中位回合 | 18 | 19 |

### 12.7　下一步

阶段 5 开局与养成节奏（部分已在阶段 2 完成）、
阶段 6 立绘接入（管线就绪，等剩余天罡出图）、阶段 7 收尾。

---

## 13　阶段 5：开局与养成节奏（已完成）

### 13.1　先跑一遍，再谈节奏

前面几个阶段都是拿「标准玩家阵容」估算的。阶段 5 先写了 `test/playthrough.js`：
用引擎自己的函数从头打到尾——按顺序推关、赢了结算、银两先督练后抽卡、
有好装备就换上、打不过就多练几级再来。第一次跑完的结果是**第 2 关就卡死**。

估算模型和真人之间差着一整套获取链，下面每一条都是跑出来的，不是想出来的。

### 13.2　跑出来的六个洞

| 洞 | 现场 | 处理 |
|---|---|---|
| 第 1 章四关首通给的是「小喽啰、悍匪、朱武、史进」——后两个是起手角色 | 整章只新增两个杂兵 | 换成陈达、杨春；第 6 章两处杂兵换成呼延灼、徐宁 |
| 经验全程只有 19.7 万，而升到 50 级要 627 万 | 光打关卡最多到 24 级，真正在升级的是银两 | 经验曲线重排（`120L + 1.2L²`，满级累计约 19.5 万），关卡经验随推荐等级涨；「升级」改成「督练」：花银两补足到下一级还差的那点经验 |
| 装备只靠 10–20% 的掉落 | 打到第 9 章还是赤手空拳，而难度是按「每格都有本章档次的装备」调的 | 首通必得一件，品质按章设限（1–4章良、5–9猛、10–15名、16–23天罡、24+绝世）；原来第 1 关就可能掉绝世兵器 |
| 升星只认「那个人自己的碎片」 | 三百七十抽打完全程，主力星级钉死在 2.7——常用的恰恰是最稀有的那几个 | 加通用碎片「兵符」，首通给（支线 2、Boss 4、隐藏 6），任何人都能用 |
| 新入伙的人从 1 级开始 | 第 20 章抽到卢俊义也顶不上一个练满的陈达，三百五十抽下来主力一个没换 | 新人从「队伍平均等级 −2」起步 |
| 隐藏关挂在 req_stage 链上 | 第 26 章的秘藏打不过，后面五章全锁死 | 前置关一路往回找到第一个非隐藏关 |

另外补了一条兜底：**败仗给三成经验**。原来输了什么都不给，打不过就是永久卡死。

### 13.3　调参器改成照着真人调

阶段 3、4 的玩家模型是估的：`obtainable[ch]` 把「这一章之前所有可能掉落的角色」
按品质排序取前九——那是一支不存在的队伍。真实推进到第 11 章时它是

```
模型：鲁智深 林冲 栾廷玉 呼延灼 史文恭 卢俊义 关胜 史进 杨志　全 ★3　胜率 85%
真实：林冲★3 鲁智深★1 杨志★4 晁盖★4 史进★4 栾廷玉★1 公孙胜★1 朱武★2 陈达★1　胜率 5%
```

战力只差 13%，胜率差 80 个百分点——这是阶段 4 发现的「断崖」在作祟。

现在改成闭环：`playthrough.js --profile` 用真实难度死磕一遍全程，
把每一章的**阵容原样留档**（谁、几级、几星、会哪几招、身上穿什么），
调参器直接照搬那支队伍，一点不估。跑三轮就收敛。

同时给调参器加了两道封顶：强度系数 ≤6 倍、敌方等级 ≤60。
不封顶的话第 25 章「洪太尉误走妖魔」这种开篇桥段会被堆到
**80 级 ×21.5 倍的潘金莲**才够卡人——数字荒唐，而且玩家在这一章被掏空，
下一章就抬不起头。撑不到目标的关就让它偏易，中间来一段轻松的过场没什么不好。

封顶之后有 14 关顶着 6 倍还是百分百胜，清一色**只有 7 个敌人**而玩家是 9 个。
人数差补不回来，于是 `data/rebalance_lateflank.py` 把第 10 章起的支线 / Boss /
隐藏关补到 9 人，补位取同章名将，杂兵不超过四分之一。

### 13.4　结果

摸底跑出来的养成曲线（真实难度，139 关全通）：

```
章   推荐级  实际级  星   队伍  拥有  装备  抽数
 1      3      3    1.0    2     3     1     0
 5      9     11.3  3.1    9    12    19     0
10     17     19.2  3.9    9    30    38    10
15     25     28.9  4.2    9    65    66    50
20     33     40.2  4.9    9   125   101   150
25     41     48.8  5.0    9   165   134   300
31     50     50    5.0    9   184   153   390
```

等级贴着推荐等级走（高 1–5 级），星级第 4 章到 ★3、第 13 章 ★4、第 21 章 ★5，
装备与角色稳步增长，抽卡全程约 350 抽、收齐七成角色。

稳定性复核（四局独立模拟，允许卡关时回头刷已通关卡）：

| | 阶段 4 收尾 | 阶段 5 收尾 |
|---|---|---|
| 全程通关 | 打到第 2 关就卡死 | **3/4 局 139/139，1 局 135/139** |
| 主线卡关 | — | **0 处** |
| 难度落进目标区间 | 138/139 | 130/139（其余为封顶后的过场关） |
| 回归 | 22/22 | **22/22** |

---

## 14　阶段 6：立绘（天罡三十六已完成）

36 张 3:4 原图（1086×1448）过流水线：绢底色温拉齐到全组平均（限幅 ±12%）、
整体向纸色 `#e9e2d0` 提亮 35%、0.5px 高斯降噪、WebP q62 压到 480×640。
单张平均 29KB，36 张合计 1.05MB，base64 内嵌后整个 HTML 1.87MB。

一个角色只出一张，三处显示全靠 CSS 缩放：人物牌满铺、详情页 150px、
战阵按上阵人数切 88 / 60 / 46px。九人阵里 46 像素的格子上人仍旧认得出来——
林冲的豹头配枪、鲁智深的光头禅杖、吴用的儒巾、秦明的狼牙棒、花荣的弓。
没出图的角色继续走兵器剪影兜底。

剩余：72 地煞 + 主要 Boss。

---

## 15　下一步

阶段 7 收尾：UI 打磨、版本号统一、全量回归、存档兼容说明。

---

## 16　演出节奏返工（阶段 4 补课）

实机反馈：**一瞬间就打完了，都不知道怎么赢的**。量了一下，确实如此：

```
关卡类型   回合数 p10/中位/p90   每回合事件数   每回合耗时
普通关         3 / 9 / 18          17.2          1.03s
Boss 关        8 / 21 / 30         20.2          1.09s
支线关        13 / 22 / 30         22.2          —
```

一回合有十七到二十三件事，全挤进一秒里 —— 每件事分到 58 毫秒。
谁出的手、打的谁、死了没有，一样看不清。
而且普通关有一成在三回合内结束，三秒钟整场就没了。

### 16.1　从「事件」改成「拍」

原来是把事件平铺成序列，一件一件放。现在先切成**拍**：

> 一个人出一次手，连着他这一手造成的全部伤害，算一拍。

十七件事平铺是十七下心跳，切成拍就是九下，每下里面是一件完整的事 ——
「林冲 · 丈八蛇矛 → 王庆 −1.1万、李助 −1.5万、王庆 −1.2万」同时落下，
因为它们本来就是同时发生的。

### 16.2　一拍停多久，看这一拍值不值

```js
w = 1
  + 0.55  有技能名（得给人读的时间）
  + 0.45  群攻（命中超过两人）
  + 0.6   暴击
  + 0.3   有治疗
  + 1.1×  阵亡人数
```

例行的一刀一枪扫过去，出了人命的拍子停下来让人看见。
基准 150ms/拍，回合数上去之后再自然收一收（7–12 回合 ×0.8、13–20 ×0.62、21+ ×0.5）——
开场看得清，鏖战不拖沓。

另外把高亮的存活时间绑到这一拍的长度上。原来金边活 560ms 而拍子只有 250ms，
三拍的金边叠在一起，还是看不出谁在出手。改完之后同时出手的格子**恒为 1 个**。

### 16.3　战报跟着画面走

引擎给每条事件记下「此刻战报写到第几行」（`Battle.ev` 统一打戳），
播放时逐拍往下写。原来是一回合结束整段刷出来，一秒钟刷二十行等于没写。
实测行数随时间 3 → 4 → 14 → 27 → 33 → 41，是一行行长出来的。

### 16.4　三档速度 + 速战

「快进」一个开关不够用。改成 **缓 / 常 / 疾** 三档，存进存档，右上角随时切：

| | 普通关（5 回合） | Boss（15 回合） |
|---|---|---|
| 缓 ×1.6 | 13.7s | 76s |
| 常 ×1.0 | 10.6s | 33s |
| 疾 ×0.26 | 2.4s | 约 9s |

换挡立刻见效：作废排队中的拍子，画面对齐当前战况，马上进下一回合 ——
否则按了「疾」还得等这一回合按旧节奏磨完（实测切档后 1.5 秒推进 3 个回合）。

通过的关卡另给一个**速战**按钮：直接出结果，不看演出，回头刷关不用再看一遍。

回归 22/22，三局通关模拟 139/139 全通。

---

## 17　开场与主线（新增）

### 17.1　立意：误走的妖魔

31 章的关卡名跨了三部书——水浒（聚义→招安→征方腊）、荡寇志（梁山被剿）、
说岳（靖康→风波亭）。三部书里不变的那个东西是**朝廷**：
一代代能打仗的人都被它用完了。

开场从**洪太尉揭开伏魔殿封皮那一刻**起笔。理由有三：

1. 它是原著真正的第一回，是整个故事的因。
2. 一百单八个名字散落人间、等人一个一个收回来——和收集玩法严丝合缝。
3. 它留了一个书里从没给准话的问题：**朝廷叫他们妖魔，百姓叫他们好汉，
   他们自己只想做忠臣**。这三样能不能是同一件事，正好用 31 章去回答。

主题不在开场点破，而是逐章加重：第 9 章「碑从地里起出来，名字早刻好了」，
第 12 章「旗号换了，人还是那些人」，第 16 章「回来的不到三成」，
第 18 章「剿它的人，论本事论人品，也都是好汉」，
第 23 章「莫须有三个字，和当年伏魔殿上那张封皮一样——都是给人看的」。

### 17.2　三个部件

| 部件 | 做法 |
|---|---|
| 开场滚动字幕 | 28 段，从嘉祐三年瘟疫写到「你打到最后就知道了」。约 40–75 秒，时长按内容实际高度算，短屏长屏观感一致。**收尾停在片名上**，不把字推出画外（推空了就是十几秒空屏），停住后自动进主界面 |
| 章首题词 | 31 张，进一章的头一关时出一张：章号 + 二字题 + 两句。看过就不再出，存进存档 |
| 下一步引导 | 主界面常驻一条，按存档状态算出该干什么，点一下直接跳过去 |

引导的判定顺序（纯函数 `Guide.next()`，不改任何状态）：

```
没打过任何关          → 先打第一关：史家村学艺
阵不满且有人闲着      → 去布阵把空位填满
阵满但板凳上有更强的  → 去布阵换一换（战力高出 15% 才提）
包里有比身上好的装备  → 去行囊换
队伍低于下一关推荐等级且银两够 → 去群将督练
碎片加兵符够升星      → 去升星
银两够十连            → 去酒肆
以上都没有            → 下一关：xxx
```

### 17.3　石碣

开篇说「名字收齐那天，石碣会从地里起出来——上面的字，早就刻好了」。
图鉴页顶上就立着那块碑：**石　碣 / 伏魔殿下掘出，字先刻好，人后来到 / 7 / 260**。
下面是密密麻麻的「？」格子，收到一个亮一个。意象和玩法在同一个页面上闭环。

### 17.4　文本存放

全部写进 `gameData_v10.json` 的 `story` 段（`intro` / `chapters` / `epilogue`），
不在代码里硬编码，改词不用动 src。存档新增 `seenIntro`、`seenCh`。

主界面有「重看开篇」。旧存档升上来会看一次开篇，这是有意的。

回归 22/22，两局通关模拟 139/139 全通。

---

## 18　星级与兜底立绘

### 18.1　升阶改五角星

`stars()` 从 `●○` 换成 `★☆`，金色。人物牌与详情页同步。

### 18.2　立绘改成三级回退

```
本人立绘（PORTRAITS）→ 范式立绘（ARCHETYPES，多人共用）→ 兵器剪影
```

九十多个偏将、枢密、先锋逐个出图不现实，也没必要 ——
玩家在 46 像素的格子里看的是「这是个什么人」，不是「这是哪一个人」。

`data/assign_archetype.py` 按「身份 + 兵器 + 是否番将」把全部角色归成十二类：

| 范式 | 人数 | 其中真正靠它兜底的 |
|---|---|---|
| arch_gen_dao 偏将·刀 | 87 | 28 |
| arch_sol_dao 军校·刀 | 42 | 9 |
| arch_gen_qiang 偏将·枪 | 35 | 12 |
| arch_civ_lit 文吏 | 23 | 16 |
| arch_gen_fu 偏将·钝器 | 21 | 7 |
| arch_liao 番将 | 15 | 4 |
| 其余六类 | 37 | 18 |

番将按姓名用字认（耶律／兀颜／完颜／粘罕／哈迷…），文职按称号认，
其余按兵器分刀、枪、钝器。改了角色数据重跑一遍即可，不用手工维护对照表。

杂兵六个不走范式 —— 数据里本来就是同一个 id，一人一张天然复用。

### 18.3　复用不穿帮

同一张范式图给几十个人用，全一个样子就穿帮了。前端按角色 id 做稳定哈希
取六种变体：**镜像翻转 × 轻微冷暖偏移**，幅度压在 6% 以内，不跳出山水古朴的色系。
最大的一类 63 人摊到六个变体，每种约十人，格子里足够看出不是同一个人。

实测变体分布（arch_gen_dao 63 人）：8 / 19 / 8 / 7 / 8 / 13 —— 够散。

### 18.4　出图口径

范式图和主角图的画法反着来：

- **不要独有记号**。疤、青记、肉瘤是辨识锚点，共用图上出现就成了
  「这十个人脸上都有同一块疤」。
- **脸往「看不太清」做**：侧脸、低头、斗笠阴影、面巾、头盔压眉。
  不是画糊，是让视线落在装束与兵器上。
- **年龄给区间不给数字**，姿态中性不带情绪 —— 情绪是主角的特权。

18 条 prompt 在 `立绘生产规范_杂兵与范式.md`。

### 18.5　两条流水线合并归一

`build_portraits.py` 现在同时处理两个来源：

```
assets/portraits/source/      → web/    一人一张
assets/portraits/arch_source/ → arch/   范式十二张
```

绢底色温**对着两个文件夹合起来的平均值**拉齐。分开跑的话两组各自归一化，
范式图会和天罡图差半档，混在一屏里一眼看得出来。

`build.py` 把 arch/ 也内联进 `window.ARCHETYPES`。

---

## 19　文案复核

「两　阵　对　圆」是章回小说的套话，今天没人知道它是「两军列阵相对」。
把全部 UI 文案抓出来过了一遍，按两条线筛：**生僻字**、**文白混搭**。

### 改掉的

| 原文 | 改成 | 问题 |
|---|---|---|
| 两　阵　对　圆 | 对　阵 | 章回套话 |
| 殁（阵亡印） | 亡 | 生僻字 |
| 督练 · N 银 | 操练 · N 银 | 生造词 |
| 将魂碎片 | 武将碎片 | 手游黑话 |
| 甲胄（护甲槽） | 战甲 | 「胄」本义就是头盔，另立一个「盔」槽，名字本身是错的 |
| 盔 | 头盔 | 同上 |
| 相契（羁绊档） | 熟识 | 偏文 |
| 终（Boss 角标） | 王 | 「终」表示 Boss 太绕；普通关的「正」直接去掉，不必加角标 |
| 整顿行伍，练级换装 | 整顿人马，练几级、换身装备 | 「行伍」配「练级换装」，文白混搭 |
| 三十合不下，以伤亡论 | 三十回合没分出胜负，按伤亡算 | 「合」是回合的古称 |
| 近事 | 最　近 | 作标题生硬 |
| 盾吸 N | 护盾挡下 N | 简写 |
| 得盾 N | 护盾 +N | 同上 |
| X 抵住了 | X 稳住了心神 | 含糊，看不出抵住了什么 |
| X 被震慑 | X 眩晕 | 格子上的状态印是「眩」，战报写「震慑」对不上 |
| 败中长见识 经验 +N | 打输了也长经验 +N | 绕 |
| 第 N 回（战斗抬头） | 第 N 回合 | 章回语境里「第三回」是第三章的意思 |
| 请张天师祈禳（开场） | 请张天师作法驱瘟 | 生僻字 |

### 留下的

**罡**（天罡、品质印）—— 水浒的核心词，开场字幕里有「三十六员天罡、
七十二座地煞」做铺垫。**碣**（石碣）—— 刻意的贯穿意象，图鉴页上就跟着
一行「伏魔殿下掘出，字先刻好，人后来到」。这两个字是有交代的，不算莫名其妙。

关隘、行囊、酒肆、兵符、受创、鸣金收兵、空空如也、稳操胜券这一类，
是常用词或常见成语，撑住了水浒的语感，也不妨碍理解。

### 顺手修的一处可读性

战报里第一章就会出现「史进 → 史进 受创 331」—— ch1_boss 的敌方阵容里
有史进和朱武，正好是玩家的起手两人。现在只给**两边都有的那个名字**
加标记：「史进（敌） → 史进（我） 受创 331」，其他人照旧不带尾巴。

回归 22/22。

---

## 20　伤势（方案甲）

### 20.1　为什么不做体力

原来一场仗打完什么都不留下：全员满血复活，输了重来一遍就是。
难度形同虚设，一百单八将也只用得着最强的九个 —— 实测第 20 章拥有 116 人上阵 9 人，
到第 31 章拥有 174 人闲着 165 个。

体力解决不了这个。体力是手游的留存机制，它不让游戏变难，只让人等，
而且不产生任何决策。真正该问的是：**一场仗打完，什么东西应该留下来。**

### 20.2　规则

| | |
|---|---|
| 阵亡 | 重伤，不能上阵，休养 3 场 |
| 结束时血线 < 25% | 轻伤，能上阵但属性 ×0.8，休养 2 场 |
| 休养的计量 | **出战场次**，不是现实时间 |
| 谁恢复 | **只有没上阵的人** —— 这一条是逼轮换的关键 |
| 治疗 | 请郎中立即痊愈。重伤价 ≈ 2.5 级操练钱，安道全在册打四折 |

重伤者自动抬下阵，位置由板凳上最强的顶上 —— 一场大败之后让玩家手动补八个空位
是白费功夫，何况他也只会这么补。

### 20.3　两道保底

**不许打成死局。** 全军覆没时先算这一仗最多能让多少人重伤，
使「还能上阵的人」不低于一阵之数（手上不够九个就按手上的算），
超出的名额从战力最高的人开始减免。实测：

| 拥有 | 重伤 | 轻伤 | 还能上阵 |
|---|---|---|---|
| 2 / 5 / 9 | 0 | 全部 | 全部 |
| 14 | 5 | 4 | 9 |
| 30 | 9 | 0 | 21 |

保底只在名册薄时起作用，中后期是全额惩罚。

**换不下来的时候伤要自己好。** 手上人数还不够摆一阵时人人都算休养 ——
不然开局两个人连打十场，朱武会一直挂着轻伤好不了，这是死结。

### 20.4　难度重新标定

伤势让玩家强度在同一关上下浮动很大。照着某一次快照调，下一局换一批人在养伤就对不上
—— 标定回路第一轮的三局复核是 97 / 97 / 133 关，全在乱跳。

改法：调参器按「**主力有两个重伤在养、还有一个带轻伤上阵**」建阵，
把波动吃进目标里。满状态时略轻松，正轮换时刚好。

顺带修了模拟自己的两个坏毛病：一有闲钱就十连（轮到伤员时账上永远是空的）、
卡关了还在抽卡。真人不会这么花钱。

### 20.5　结果

| | 伤势之前 | 伤势之后 |
|---|---|---|
| 通关模拟 | 3/3 局 139/139 | **5/5 局 139/139** |
| 打完全程的战斗场数 | 约 270 场 | **310–370 场** |
| 全程治伤开销 | — | 5,000–9,600 银 |
| 回归 | 22/22 | **23/23**（新增伤势一项） |

多出来的八九十场仗全部来自轮换与重试，没有一处变成墙。

首通银两上调 20%：伤势多了一个花钱的口子，不松一点抽卡这条线会整个饿死。

### 20.6　还需要看着的一点

现在银两的三个出口是操练、治伤、抽卡，而操练吃掉九成。
轮换意味着要养一条更深的板凳，这条板凳也要练级 —— 这正是「深度阵容变得值钱」
的代价，逻辑自洽，但实测两局里抽卡次数从 110 抽到 0 抽都有，波动偏大。
真人玩起来如果觉得抽卡被饿死，再把首通银两往上抬一档即可，不必动伤势本身。

---

## 21　阵型四改

### 21.1　前排优先承伤

单体攻击先锁活人最靠前的那一排，那一排清空了才轮到下一排；排内仍优先打正对面。
群攻（整排、整列、全体）不受约束，本来就是照着面打的。

这一条让九宫格第一次真的是「阵」。实测同一关打 30 场：

| 排法 | 胜 | 前排挂彩 | 后两排挂彩 |
|---|---|---|---|
| 按战力排 | 30/30 | 43 | 56 |
| 耐揍的放前排 | 30/30 | 29 | 39 |

胜率没变，**挂彩少了三成**。阵型的回报落在伤势上——排得好，养伤的人就少。

顺带一个副作用要记下来：伤害集中在前三个人身上之后，后排几乎不掉血，
整体减员变少，通关所需战斗从 310–370 场降到 177–234 场。
也就是说「会排阵」实打实地降低了难度，这是设计要的，不是漏的。

### 21.2　拖动换位

不用 HTML5 的 dragstart —— 移动端支持得一塌糊涂。统一走 pointer 事件：
**按住 180 毫秒才算「拿起来」**，免得轻轻一点就被判成拖拽、反而进不去详情页。
按住之前手指滑出 12 像素就取消，当成翻页。

一个坑：拖起来的格子必须 `pointer-events:none`，
否则 `elementFromPoint` 永远返回它自己，落点判不出来，拖了等于没拖。

阵列不留空洞——位置就是出场次序，前三个站第一排。留空洞的话战斗里的行列
要另算一套，不值当。

### 21.3　一键上阵

挑当下能打的最强九人（重伤的自动剔除），再按「血 ÷ 5 + 防 × 2」把耐揍的三个
排到前排，其余按战力排后面。前排优先承伤之后，谁站第一排不再是无所谓的事。

### 21.4　行囊列全部装备

原来只列背包里闲着的。现在背包的 + 穿在人身上的合成一张总表，
穿着的标明「谁在用」，点一下跳到那个人的详情页。抬头写「闲置 N · 在用 M」。

---

回归 23/23，三局通关模拟 139/139 全通。

### 21.5　小节折叠

抬头点一下收起，存进存档，刷新还在。能折的：群将谱、关隘每章、布阵的已激活羁绊、
行囊三段、羁绊谱、图鉴按来源分的三组、主界面的「最近」。

收起时**内容干脆不生成**，不是 `display:none` 藏着。群将谱折起来 DOM 少 395KB
（立绘是 base64 内联的，一张人物牌就几十 K），翻页明显快。

### 21.6　前中后排标签放错了位置

上一版把「前排／中排／后排」做成了三个**列**头，但引擎里 `row = floor(idx/3)` ——
上面三个才是第一排。逻辑没错，字放错了地方。

改成每行左边一条竖排的标签栏（前排朱砂色，带「先挨打」小注），
现在屏幕上看到的和引擎算的是同一回事。核对过：一键上阵把最耐揍的三个放在
`G.team[0..2]`，战斗里 `row===0` 的正是这三个人。

---

## 22　范式立绘接入（第三批美术资产）

### 22.1　到货与处理

十二张范式底图，1086×1448、3:4。和前两批走同一条流水线
（`build_portraits.py`，绢底拉齐 → 向纸色提亮 35% → 0.5px 降噪 → WebP 480×640），
绢底色温跨 `source/` 与 `arch_source/` 一起对齐，所以范式图和自有立绘摆在一起
看不出是两批画的。

至此 266 名武将全部有脸：自有立绘 42 张，范式兜底 224 人，
第三级的兵器剪影再没有人走到——实测 `svgFallback 0`。

### 22.2　一张图顶几十个人：取景比色调管用

范式立绘按 hid 稳定哈希取八种变体。原来八种全靠镜像加轻微冷暖，
八张摆一排看过去还是同一个人——脸没变，滤镜改不了脸。

试过把色相转大（±13～18 度）。**失败**：绢底跟着一起变色，
卡面背景出现粉的绿的，立绘从界面上浮起来了。绢底的统一色温正是前两批花力气
做出来的东西，不能为了区分度拆掉。

改成**换取景**。`build_portraits.py` 给每张范式底图多出一张半身特写
（取原图上方 58%，裁成 3:4 再拉回 480×640）。全身像和胸像是两种构图，
隔着两格也看得出不是一张卡。八种变体因此变成：

| | 全身底图 | 半身底图 |
|---|---|---|
| 正／镜像 | v0 v1 | v4 v5 |
| 正／镜像 + 轻微冷暖 | v2 v3 | v6 v7 |

色偏压回 6% 以内，只动亮度和 sepia，不碰色相。
范式图从 12 张变 24 张，内嵌 333KB → 747KB，整包 2.47MB → 2.88MB。

### 22.3　镜像改用独立的 scale 属性

原来镜像写的是 `transform:scaleX(-1)`，而出手／中招／倒地的动画也写 transform，
动画一跑镜像就被顶掉——打一下翻一次脸，之前一直没注意到。

改成 CSS 的独立 `scale:-1 1`，它和 `transform` 各走各的，互不覆盖。
核对过：加上 `.cell.acting` 之后 `scale` 仍是 `-1 1`，`transform` 另走 lean 的矩阵。

### 22.4　归类订正

`assign_archetype.py` 原来靠「身份 + 兵器 + 是否番将」的正则归类，
兜底一律进 `arch_gen_dao`，结果神医安道全、兽医皇甫端、账房蒋敬、
道人包道乙都披了一身甲提着刀。共用一张脸只是「像」，穿错行当是「错」。

加了一张点名表（`NAMED`），十五个人直接指定；再补一条「title 含『女将』→ 女性范式」
（于玉珏、卜树英原来是男将模样）。`禅杖` 并进钝器，邓飞不再算刀将。

订正后走范式的分布：

```
arch_gen_dao 48　arch_sol_dao 41　arch_gen_qiang 31　arch_civ_lit 29
arch_gen_fu  21　arch_liao    15　arch_woman     11　arch_civ_mkt 10
arch_monk     8　arch_elder    4　arch_gen_jian   4　arch_sol_qiang 2
```

`arch_gen_dao` 仍是最厚的一档（48 人，含晁盖、关胜、史文恭、方腊这些有名有姓的）。
再往下分只是把厚度挪到另一张图上，治不了本——真要治，得给这几位出自己的立绘。

### 22.5　核对

回归 23/23；三局通关模拟 139/139 全通。
46 像素战阵格、布阵九宫、详情页 146px 三处都看过，范式图和自有立绘同色系，
没有穿帮。

---

## 23　手机端适配与整齐度、游戏性、文案 四轮审计

### 23.1　手机端适配

先说没问题的：320 / 360 / 375 / 390 四档宽度逐页量过，**零横向溢出、零出界元素**。

改掉的：

- **安全区**。`viewport-fit=cover` 开着，全文却没有一处 `env(safe-area-inset-*)`，
  `.app` 只有写死的 `padding-bottom:70px` —— iPhone 底部小黑条压住页尾按钮。
  四边改成 `env()` 相加。
- **`min-height:100vh` → 加一行 `100dvh`**。地址栏收起时不再跳。
- **`overscroll-behavior-y:contain`**。原来演出中手一滑就触发下拉刷新，这一仗白打。
- **点击热区**。下阵的「×」只有 17 见方，旁边就是拖拽区；「配」28×26；
  战斗中要点的速度档 32×24。「×」的印章保持小，热区用伪元素撑到 44；其余直接给足。
- **字号**。战斗格里的状态印 8.6px、血量 9px、战报 9px，手机上看不清，一律提到 10.6 以上。

### 23.2　整齐度

- **导航七个入口** `flex-wrap` 断成 5+2，前五个 64px 后两个 169px。改 `grid` 四列两行等宽。
- **图鉴石碣**格子宽度随名字长短变，两字三字「？」三种宽度混排，右边缘全是参差。
  改六列定宽，四字五字的名字自动降字号。一块碑总算像一块碑。
- **酒肆品阶** chip 同样的毛病，改三列两行。
- **小节抬头的横线**原来是 `flex:1`，右侧附注一长一短，两个小节的线就错开。
  给附注定宽 8.8em，线一律收在同一处。
- **关卡页战力对比**改三等分栅格加 `tabular-nums`，四位数五位数混排也不跳。

### 23.3　游戏性

**操练从一级一级点，改成可以连练。** 一周目操练要花二十多万银，
按每级百来银算是上千次点击。新增「连练到 N 级」，按钮上直接写手上的钱能练到几级。

**督练封顶**（`Grow.drillCap`）：只能练到已通关卡推荐等级 + 3。这一条同时解决两件事 ——

1. 操练原来是个无底洞，银子永远先被它吃光，酒肆那边一次也抽不起。
   三局模拟的抽数都是 **0 抽**。试过加关卡收入、试过把操练单价斜率砍半，
   都没用：只要操练还能点，玩家（和模拟器）就会一直点，永远轮不到抽卡。
   封顶之后银子自然溢出，三局模拟变成 **20 / 80 / 250 抽**。
2. 堵住了拿钱把等级堆过头、直接抹平关卡难度这条路。

设定上也讲得通：本事是打出来的，钱只能把该有的阅历补齐，补不出没打过的仗。

**其余**：买不起的招贤按钮直接置灰；群将谱加排序（战力／品阶／等级／星数）
与筛选（全部／在阵／未上阵／带伤／可升星）；「重开一局」从浏览器原生 `confirm()`
换成自绘弹层 —— 原来那个不光跟界面两回事，装成主屏应用之后有的壳子干脆不弹。

### 23.4　文案：139 关全部重写

审计里最实的一处。原来 103 关的对白是三句模板灌出来的：

- 第一段按章复用，103 关只有 **26 种**正文，同章每关一字不差
- 第二段套 `此去{章节名}` —— 于是有了「**此去荡寇志·起**，正是英雄用武之时」
  「此去开篇·妖魔出世」「此去征方腊·上」，103 关全中
- 第三段「战鼓催征，旌旗猎猎……」**103 关完全相同**
- 胜负白也是 103 关完全相同，而且「{关名}一战，梁山**（岳家）**大获全胜」——
  括号里那个注释是写给自己看的，直接漏给玩家了

现在 139 关各写各的：关前两段、胜一句、负一句，共 556 段。
写在 `data/copy/part1–4.py`，由 `data/apply_copy.py` 写回并自检 ——
四个字段各自去重必须是 139 种，且不许残留「此去」「（岳家）」「战鼓催征」
「正是英雄用武之时」。

顺带：战报里的伤害原来不走 `num()` 折算，格子写「27万」战报写「93132」。
`num` 从 view.js 挪进 engine.js，两边共用。

### 23.5　核对

回归 23/23；通关模拟 139/139，全程无卡关；四档宽度复测，小热区与小字告警清零
（只剩「×」的 19px 印章，已用打点验证其实际热区约 37×37，向内三个方向都够）。

---

## 24　二周目「重整旗鼓」

### 24.1　为什么是二周目

一周目打完那一刻的三条成长线：等级 47/50（撞顶）、星级 4.9/5（撞顶）、
收将 89/260（34%）。符的账对得上 —— 一周目首通合计 302 符，
一人从 1★ 拉到 5★ 要 30 符，正好够拉满一队十个人。
所以星级不是没练，是给的量刚好只够一队。

加关卡单独做不成立：两个上限都封着，新关卡的敌人要么白送要么打不过，
而打不过也没法靠练解决 —— 加关卡必须跟提上限捆在一起。
整体提高难度则会推平已有的闭环校准，增加的是「打不过就回去刷」的时间。

二周目复用 139 关、556 段文案、全套敌方配置，不新写内容，
一次性解开两个撞顶的成长轴。

### 24.2　周目字段与存档兼容

`G.lap` / `G.fates` / `G.everCleared`。老存档没有这三个字段，
读进来按一周目算 —— 回归里有一条专门盯这个（见 24.6）。

通关 139/139 后主界面出现「重整旗鼓」。保留武将、等级、星级、装备、行囊、
银两、符、碎片、图鉴；清零关卡进度与伤势；`lap += 1`；抽三颗宿星。

`everCleared` 跨周目不清：上一周目打过的关，这一周目直接可以速战跳演出。
不然重看一遍 139 段关前白很烦。

### 24.3　上限与星级续档

| 周目 | 等级上限 | 星级上限 |
|---|---|---|
| 一 | 50 | 5 |
| 二 | 60 | 6 |
| 三及以后 | 70 | 7 |

★6 ★7 不发新技能（技能位在 ★2/3/4 已经发完），只续属性与技能威力：
`starMul` 续 1.80 / 2.02，`starSk` 续 1.26 / 1.32，`starCost` 续 32 / 64。
一人从 ★5 拉到 ★7 要 96 符；二周目符的产出按周目翻倍，一趟约 600 符。

### 24.4　敌方不是「乘」，是「整段平移」

这是这一节里最要紧的一条。二周目玩家是**带着满配从第一关重打**，
而关卡曲线是给「从零长起来」设计的 —— 一周目的敌方等级从 3 爬到 60。
拿满级队去打 3 级的小喽啰没有意义，光乘一个系数也救不回来：
第一章要有挑战就得乘几十倍，末章就该爆了。

所以敌方等级从一周目的 3–60 **线性映射**到本周目的区间
（二周目 46–60，三周目起 56–70），关卡之间的相对难度原样保留；
星级每周目 +1，上限与玩家同档；平移之外再乘一道微调 `lapFoeBy`。

推荐等级跟着一起平移 —— 不然二周目第一关会写着「推荐 1–3 级 · 敌方 46 级」。
督练上限也吃平移后的推荐等级，否则二周目大半程练不动
（另加一条兜底：上一周目挣来的等级就是这一周目的底）。

### 24.5　微调系数：查表，不用幂次

二周目 **1.18**，三周目起封顶 **1.30**。

一开始写的是 `pow(lapFoe, lap-1)`，三周目就是 1.18² = 1.39 ——
而玩家从二周目到三周目只多 17% 等级上限加 12% 星级倍率，
敌方涨得比人快，三周目停在 136/139、打了 1524 场。改成查表。

三周目起玩家数值封顶（70 / ★7），敌方也必须跟着封顶，
否则第四周目必死。往后各周目靠宿星换手感，不靠堆数值。

校准结果（每档三局或两局，`playthrough.js --lap N --foe X`）：

| lapFoe | 二周目结果 |
|---|---|
| 1.00 | 139/139，但只打 139 场，零重试零伤员 —— 白送 |
| 1.18 | 139/139 ×3，场次 291 / 139 / 139 ✓ |
| 1.22 | 三局里一局停在 136/139 |
| 1.35 | 撞墙 111/139 |

窗口比一周目窄得多，因为二周目玩家的战力区间被上限卡死，
不像一周目那样一路长上来。

### 24.6　宿星

二周目开局石碣亮三颗，全程生效，周目之间不继承。
每颗可花 30 符重掷一次，只一次 —— 每条都有得有失，没有纯赚的，
所以重掷是权衡不是刷分。

| 宿星 | 得 | 失 | 挂在哪 |
|---|---|---|---|
| 天魁星 | 我方武力 +12% | 敌方血量 +15% | Stats.calc / calcEnemy |
| 天杀星 | 我方暴击率翻倍 | 敌方暴击率也翻倍 | Battle.critOf |
| 天速星 | 我方捷 +18% | 我方防 −10% | Stats.calc |
| 天寿星 | 休养少一场 | 治伤价钱翻倍 | Hurt.settle / cureCost |
| 天富星 | 银两 ×1.6 | 符产出减半 | Stages.settle |
| 天巧星 | 装备掉落 ×2 | 装备品阶上限降一档 | Stages.settle |
| 天哭星 | 银两 ×1.8 | 阵亡一律重伤 | Hurt.settle |
| 地孤星 | 我方全属性 +15% | 羁绊全部失效 | Stats.calc |
| 地煞星 | 首通符 +2 | 敌方每关多一人 | Battle.create |
| 地损星 | 首通经验 ×1.4 | 每关结束随机一人轻伤 | Stages.settle |
| 地伏星 | 前排承伤减免 20% | 后排受创 +20% | Battle.hurt |
| 地灵星 | 每回合全队回血 2% | 安道全不在册时全队血量 −8% | Battle.runRound / create |

`Fate.v(key, dflt)` 是统一取值口：兜底是 1 就相乘、是 0 就相加，
多条宿星叠在同一个 key 上也不会打架。

### 24.7　核对

- 回归 25/25，新增两条：**周目与宿星**（上限随周目变、关卡进度清零而
  `everCleared` 留住、敌方等级确实平移、宿星真的改数、重掷扣符且只许一次、
  ★6 可升）与 **老存档照常读得进来**
- 后一条踩过一个坑：先开页、再写旧档、然后 reload 是测不出来的 ——
  旧页面的 `beforeunload` 会把刚写进去的存档盖掉。得用 `addInitScript`
  在页面脚本跑起来之前把存档种进去
- 通关模拟：一周目 139/139（177 / 177 / 234 场）、二周目 139/139
  （291 / 139 / 139 / 158 场）、三周目 139/139（139 / 538 场），全程无卡关
- 二周目顺带解决了收集：三局里抽了 310 / 500 / 890 张，收将从 89 涨到 200 以上

---

## 25　立绘规格下调与出图次序

### 25.1　为什么要压

单文件 3.0MB，构成是立绘 57%、范式 26%、数据 12%、代码样式 5% ——
**立绘占了八成三**。gzip 之后 1.9MB（GitHub Pages 自动 gzip），
所以下载不是问题，问题是手机上的解析与内存。

真正的坑在后面：按 480×640 q62 的老规格补齐 172 张，单文件会变成 **7.6MB**。

### 25.2　试过 AVIF，不用

同体积下 AVIF 与 WebP 在这种柔和的绢本画面上画质相当（480 尺寸：
AVIF q48 = 21.4KB，WebP q42 = 21.3KB），换不来便宜，却要多担一层兼容风险。

### 25.3　真正的杠杆是尺寸

最大的显示处是详情页 146 CSS px —— 就算 DPR3 手机也才 438 设备像素；
战阵格只有 46–88px。原来出 480×640 属于大材小用。

拿林冲那张做 1:1 对比（按真实设备像素解码后截脸）：

| 规格 | 单张 | 详情页看脸 | 战阵格 |
|---|---|---|---|
| 480 q62 | 29.9KB | 基准 | — |
| 420 q62 | 22.9KB | 几乎分不出 | 一样 |
| 420 q50 | 18.9KB | 略糊一点 | 一样 |
| 360 q62 | 16.9KB | 明显发软 | 一样 |
| 360 q50 | 14.0KB | 发软 | 一样 |

**战阵格上 360 和 480 肉眼完全一样**，所以约束只来自详情页那一处。
落在 **420×560 / q56**（半身变体 q52）。

结果：单文件 3.0MB → **2.19MB**（gzip 1.36MB）；
按这个规格补齐 172 张约 5.6MB（gzip 3.7MB），而不是 7.6MB。

源图都在 `source/` 与 `arch_source/`，改回去重跑一遍 `build_portraits.py` 就是。

### 25.4　出图次序改按出场次数

原来第二批规范是按「地煞七十二 → 主要 Boss → 次要 Boss」排的。
换成按**这个人在 139 关里出现多少次**排（见 `立绘出图名单.md`）：

| 出到第几张 | 覆盖玩家所见 |
|---|---|
| 现状 36 张 | 44% |
| +10 张 | 58% |
| +40 张 | 80% |
| +80 张 | 93% |
| +130 张 | 100% |

**前四十张吃掉八成**，后面九十张只换来最后两成。
出场最多的十个缺图者是岳飞（23 次）、岳云（18）、方腊（17）、金兀术（15）、
阿里奇（15）、陈希真（14）、云天彪（13）、琼妖纳廷（12）、邓元觉（12）、李助（12）——
全是反复出现的主线对手，现在打岳飞和打岳云看到的是同一张披甲将军图。

prompt 本身不用重写，`立绘生产规范_第二批.md` 里都有，只是换个出图次序。

### 25.5　以后还能省的一笔

等前 166 人都有了自己的脸，范式图只服务剩下九十来个一次性配角。
那时候可以把范式的半身变体砍掉（两个取景当初是为了几十个人共用一张图时不穿帮，
人少了就不需要），再省约 370KB。

### 25.6　核对

回归 25/25。DPR3 下逐页复看：详情页、人物牌、战阵格，与压之前看不出差别。

---

## 26　美术与对齐审计（第二轮）

逐屏逐弹层截图，配几何量化（同排按钮的宽高与间距、各层左右边缘、越界元素）。
上一轮的审计靠的是程序化检查，漏掉的正是弹层和视觉层面的东西。

### 26.1　错位

**武将详情页那排按钮被挤扁 —— 上一轮改出来的。** 实测：

| 按钮 | 宽 |
|---|---|
| 操练 · 105 银 | **32.7px** |
| 连练到 3 级 | 135.1px |
| 升星 · 0/2 | 135.1px |

`.dbar .btn.sm` 写的是 `flex:1 1 40%`。这一排的按钮个数是变的（有没有「连练」看钱够不够，
有没有「郎中」看伤不伤），两个时正好，加上「连练」就是 40%×3 = 120%，
收缩时按内容基准分配，最短的那个被压成一条。改成两列等分栅格，
奇数个时最后一个通栏，几个都一样宽。

**布阵九宫格整体右偏 42 像素。** 左边那条竖排的「前排／中排／后排」标签栏占掉 2.1em，
把格子从 x=16 推到了 x=58，而同一页的标题和按钮还在 16。
改成每一排上面压一条横标签，格子回到满宽 —— 左右和别处齐了，格子还从 95px 宽到 109px。

**小节横线长度不一。** 有右侧附注的（「闲置 13 · 在用 0」）线收在 8.8em 处，
没附注的一路拉到边，同一页上下两个小节一看就歪。给没附注的补一格空占位。

随后又踩了两个坑：占位格固定宽度时，`羁绊谱（12/118 激活）` 那个长标题把横线挤成了 **0 宽**，
占位还溢出到 374（内容区右边是 359）；给标题加 ellipsis 救了线，
章节名又被截成「第1章 · 少华山聚…」。最后定的次序是 ——
**抢宽度时标题最后让，其次横线（留 1.2em 底），先让的是右边那格附注**（`flex:0 8 8.8em`）。
320 宽下复测，没有一个标题被截。

**弹层底部没吃安全区**，按钮紧贴屏幕下缘。`.sheet` 的 `padding-bottom` 是写死的 20px，
改成 `calc(20px + env(safe-area-inset-bottom))`。

**装备多了点不到「关闭」。** 原来整个抽屉一起滚，兵器攒到十几件就得滚到底才够得着。
改成抬头与按钮钉住、中间那段单独滚。

**「配」键连成一根竖条。** 五行的按钮原来 `align-self:stretch` 撑满 44 高的行，
上下边框首尾相接。收到 32 高（间距 12），热区用伪元素补回 44 —— 打点验过 ±18 都命中。

### 26.2　文案漏出来的

**26 个关卡名带「·Boss」，5 个隐藏关名带「★」**，直接显示给玩家 ——「史进显威·Boss」。
这两个后缀是给自己看的记号，关名左边本来就有方印标着。
`stName()` 在显示时剥掉，数据里的 `name` 原样留着（调试和模拟还靠它认关卡类型）。
关隘页、关卡详情、结算副标题、解锁提示、下一步指引，五处都走它。

**六个人的绰号字段就是名字本身**（五种杂兵加潘金莲），照直显示成「潘金莲　潘金莲」。
`titleOf()` 相同就不显示。

**装备属性里两个「武」并排**：「武+32 武+16%」，前者定值后者百分比。
百分比换成双字词 —— 武力 / 体魄。

**结算里四个「首」印排成一列**。文字已经写着「首通」了，印改成各自的类别（银经器将）。

### 26.3　美术

**十连结果页是范式图撞车的重灾区。** 一屏十张人物牌，
**四个是同一张铁盔兵，两个是同一张文官**。镜像、色调、半身那套变体在别处管用，
十张并排摊开就彻底穿帮。改成一人一行：34px 小头像 + 名字 + 绰号 + 品阶 + 入伙／碎片。
撞车不再并排摆出来，也看得更快。

（中途塌过一次：五个元素塞进四列栅格，「入伙」被挤到第二行、名字折行。五列全部写死才对。）

**结算奖励行的方印上下相接**，七八条叠下来连成一根柱子 —— 给行距。

**关隘页两种标记语言打架**：Boss 是带框的「王」印，普通关是个**空的框**。
普通关改成本章内的序号（一二三四），八个印一个样式。

**章首题词整块偏下** —— 底下还有一行「轻触继续」定在下缘，扣掉它才是正中。

### 26.4　核对

回归 25/25；通关模拟 139/139 无卡关；四档宽度复测，小热区只剩「配」与「×」
两处视觉上刻意做小、热区已用打点验证到 44 的按钮。

---

## 27　行囊里的杂物：从摆设接成能用的东西

### 27.1　问题

`G.items` 在代码里**只有写入、没有任何读取消耗的路径**。行囊那个「杂物」段是个陈列柜，
每一行连 `data-action` 都没有。一周目打完会攒下：

| 物品 | 掉落处 | 期望数量 | 原状 |
|---|---|---|---|
| **黄金** | 59 | **20.6** | 当物品塞进背包，钱包一分不涨 |
| 无名碎片 | 30 | 12.2 | 无用 |
| **银两** | 14 | **5.1** | 同样进背包不进钱包 |
| 经验书（两种） | 19 | 4.3 | 无用 |
| 技能书（三种） | 14 | 2.0 | 无用 |
| 治疗药剂（两种） | 6 | 1.4 | 无用 |
| 密信（两种） | 3 | 0.1 | 无用 |
| 羁绊信物 | 1 | 0.0 | 无用 |

最要命的是黄金。结算里 `con` 掉落一律走 `G.items[id]++`，而 `silver` 和 `gold`
本身就在物品表里 —— 打完一关弹「获 黄金」，钱包纹丝不动。
黄金是「黄金求贤 · 2 金保底天罡」的唯一货币，本该有的三十个只剩每场 5% 概率那一条路上的十个。

物品表 32 条里还躺着三批从没接上的设计：**羁绊信物**（英雄数据里 `bond_item` 字段还留着，
15 个人有值，但羁绊后来改成按队伍成员算了）、**技能书**（要按攻击／防御／特效给加成，
技能管线里没这个分类）、**钥匙**（隐藏关现在靠 `req_stage`，而且 key3 key4 指向的
`hidden_lsh`、`hidden_fangla` 两个关卡**根本不存在**）。

### 27.2　接上的三样

**伤药。** 原来写「恢复 100 点 HP」，而战斗里根本没有道具回血这回事。
改成接现有的伤势系统 —— 治伤原本只能花银子，而银子被操练吃得差不多，正缺这一块：

| | 作用 |
|---|---|
| 金疮药 | 治好一人的轻伤 |
| 白药 | 治好一人的伤，轻重不论 |
| 大还丹 | 全军伤势一扫而空（不用挑人） |

**经验书。** 数值原样，只是现在真能用：点「用」→ 挑人 → 读完长经验，顺带结算升级。
挑人那一层按等级从低到高排。

**无主碎片**（原万能碎片）。升星时可顶任何人的专属碎片，
`frag / frag2 / frag3` 分别抵 1 / 3 / 5 片。扣的时候从抵得少的先花。
升星的用料次序是：本人的专属碎片 → 无主碎片 → 兵符，按钮上照这个次序写
（`升星 · 0/2 +片1 +符1`）。

### 27.3　清掉的

钥匙、技能书、羁绊信物从掉落表里拿掉，按品阶与章节深浅换成同档的有用东西
（`data/fix_drops.py`，共 18 处）。这三类仍留在物品表里 —— 哪天要做，接上就能用。

替换后一周目的期望收获：黄金 20.6、无名碎片 12.3、银两 5.1、
孙子兵法 4.6、武经总要 1.6、白药 0.8、金疮药 0.6。

### 27.4　核对

- 经验书、三档伤药、无主碎片顶升星、金银进钱包，逐条跑过：
  金疮药治不了重伤会挡下来，白药可以；大还丹一次清九人；
  升星 15 片无主碎片扣 2 片后剩 13
- 强制把某关的黄金掉落拉到 100%，结算条目是「金 黄金 +1」，钱包 +1，背包里没有金银
- 回归 25/25；通关模拟两局 139/139 无卡关

---

## 28　羁绊谱与人物表勘误

118 条羁绊逐条对了原著。错法分四种，另有十来组重复。

### 28.1　根子在人物表上

有几条羁绊错得莫名其妙，查下去是人物表本身有问题：

| 问题 | 处置 |
|---|---|
| 蒋忠 ←→ 蒋门神 | 同一个人拆成两条，名字和绰号还互换了。并成一条 |
| 郑屠 ←→ 镇关西 | 同上 |
| 张猛方（没羽箭） | 没羽箭是**张清**，此人原著无考 → 换成**何涛**（济州缉捕使臣） |
| 邰正（铁臂膊） | 铁臂膊是**蔡福** → 换成**牛二**（没毛大虫，杨志卖刀撞上的泼皮） |
| 崔珏（催命判官） | 催命判官是**李立**；崔珏是道教的判官神，不是梁山好汉 → 换成**王婆** |
| 燕顺绰号「锦豹子」 | 锦豹子是**杨林**，燕顺是**锦毛虎** |
| 萧嘉穗绰号「神机军师」 | 神机军师是**朱武**；萧嘉穗原著无绰号 → 改「布衣豪杰」 |

换人时数值与技能照搬原条目，战力曲线不动，只换名号与来历。
三个被换掉的都没有关卡引用，改起来是干净的。

`data/fix_heroes.py`。人物 266 → 264。

### 28.2　认错了人

| 羁绊 | 原成员 | 改成 |
|---|---|---|
| 拳打小霸王 | 鲁智深、**周侗** | 鲁智深、**周通** |
| 雁翎甲 | 徐宁、**史进** | 徐宁、**时迁** |
| 桃花山 | 孔亮、孔明、李助 | 周通、李忠 |
| 清风山 | 阎婆惜、宋江、张文远 | 燕顺、王英、郑天寿、宋江 |

第一条是 id 相近栽的跟头：`zhou_tong` 是周侗（岳飞林冲的师父），
`zhou_tong_xiao` 才是桃花山的小霸王周通。醉打小霸王打的是后者。
第二条：雁翎锁子甲是徐宁传家宝，时迁盗甲赚他上山，与史进无关。
第三、四条是把两个不相干的山头／地方的人凑在了一起。

### 28.3　名实不符

| 原名 | 改成 | 缘由 |
|---|---|---|
| 师徒·卢俊义 | 同门师兄弟 | 林冲卢俊义同师周侗，是师兄弟不是师徒 |
| 师徒·呼延徐宁 | 钩镰破连环 | 两人无师徒之实，徐宁的钩镰枪破呼延灼的连环马 |
| 高俅节度使 | 高俅逼走王进 | 高俅是太尉；王进是被他挟旧怨逼走的 |
| 官军败将 | 两庄主 | 李应是李家庄庄主、柴进是柴大官人，从来不是官军 |
| 相扑力士 | 智扑擎天柱 | 相扑是燕青的本事，成员也从鲁智深武松换成燕青卢俊义 |
| 梁山骁骑 | 轰天炮 | 凌振是炮手不是骑兵；彭玘换成同造火炮的汤隆 |
| 梁山步卒 | 内务二人 | 宋清管筵席、郁保四扛旗，都不在步军头领之列 |
| 北宋殿帅 | 临阵定策 | 殿帅是高俅的官职 |
| 枯树芒砀 | 芒砀山 | 项充李衮随樊瑞据芒砀山；枯树山是鲍旭的，两个山头原来拼在一条里 |
| 略胜一筹 | 校场比武 | 杨志索超在大名府斗了个平手，梁中书一起提拔，谈不上谁胜一筹 |
| 两个大和尚 | 乌龙岭双僧 | — |
| 婆惜情夫 | 郓城私情 | — |
| 奸夫淫妇 | 紫石街 | — |
| 梁山探子 | 探路先行 | — |

### 28.4　跨势力、跨书乱配

- **辽金联军·贺重宝**、**辽将贺重宝·说岳** —— **金灭辽，两国是死敌**，并掉
- **青州守将**（李助、田虎）→ 秦明、黄信。李助是淮西王庆军师，田虎是河北晋王，两人既非同势力也不在青州
- **登州祝家**（孙立、祝永清）→ 孙立、**栾廷玉**。两人是师兄弟，孙立诈降做内应破了祝家庄；祝永清是《荡寇志》的人
- **田虎谋士**（萧嘉穗、乔道清）→ 萧嘉穗是协助平定**王庆**的义士，站在田虎对面
- **阳谷旧事**（蒋门神、何九叔）→ 王婆、何九叔、潘金莲、西门庆。蒋门神在**孟州**快活林
- **牛皋结义**（牛皋、张奎）→ 牛皋、岳飞、王贵、汤怀
- **说岳抗金** —— 把《说岳》的高宠郑怀张奎、《荡寇志》的雷将、还有王婆侄女董针姑混成一条，并掉
- **宋营云氏·说岳** → **雷部三将**（云天彪、云龙、邓宗弼），原来混进了《说岳》的张宪
- **说岳金国** —— 陆文龙与曹宁都已归宋，不该还算在金营里；去掉二人便与「金国阵营」相同，并掉

保留的跨书羁绊是有意的：周侗门下的林冲、武松、史文恭、岳飞，正是把《水浒》
和《说岳》接起来的那条民间线；四大奸臣、双枪将、杨家将同理。

### 28.5　职司归错

- **步军骁将**里的扈三娘 → 她是马军三女将，换成刘唐
- **马军骠骑**补齐八骠骑（原来少了张清与穆弘）

### 28.6　并掉的重复

田虎宗族＝田氏三杰、云氏双雄⊂宋营云氏、方腊猛将＝方腊双璧、王庆悍将与王庆武将、
饮马川旧部＝饮马川、少华山双雄⊂少华山、梁山早期·聚义、智取生辰纲≈七星聚义、
师出同门⊂周侗门人、高俅门生、清风山旧部＝婆惜情夫、田虎文臣⊂田虎军师、
常州守将⊂方腊君臣、方腊文武、梁山酒店⊂江湖店主、水军副将⊂水军头领。

### 28.7　核对

`data/fix_bonds.py` 跑完自检：成员里没有找不到的人、没有只剩一人的条目、
没有成员完全相同的两条。羁绊 118 → **96** 条。
回归 25/25，通关模拟 139/139 无卡关。

数值（attr / val / tiers）一律没动，改的只是「谁和谁有关系、这关系叫什么」。

---

## 29　章号归位

起因是一句问话：「139 关分别是哪些章节和剧情？」照着章号导一遍表，
表本身就不对 —— 一个章号底下挂着两条互不相干的线。

### 29.1　查出来的五类

**一　两段孤儿关。** `ch19_1/_2/_boss` 和 `ch22_1/_2/_boss` 是荡寇志还排在
第19章、岳母刺字还排在第22章时的旧稿。后来内容前移到 17/18 和 20，新关
用 `b` 后缀写进 19/22，旧的没删，还被 `req_stage` 重新串回了链上。三个后果：

- 关名重复：「岳母刺字」「枪挑小梁王」各有两关
- 阵容逐字相同：`ch19_2 陈希真出山` = `ch17_1 陈希真上山`
- 剧情倒流：荡寇志·起 排在 荡寇志·终 后面

**二　一章两个名字。** 关卡列表的章名取 `byChapter[ch][0].ch_name` ——
只读第一关。第19章第一关是孤儿段的 `ch19_1`，于是表头写「荡寇志·起」
（第17章的名字），里面装的却是靖康耻。第22章同理。

**三　列表乱序。** `stages` 的键序决定 `byChapter` 的顺序，而这两段被塞在
`ch6_boss` 和 `ch7_1` 中间。第19章就显示成「云天彪初阵／金兵南侵／
陈希真出山／粘罕铁骑…」，两条线交错。

**四　七章无名。** 第24、26–31 章的第一关都是 `_f` 支线关，`ch_name` 空着，
表头只剩「第N章」。题词卡那边名字其实一直都有。

**五　34 关的敌人等级脱离推荐等级。** 另外 105 关都是 `enemy_lv == rec_lv[1]`，
这 34 关不是：`ch25_1`–`ch25_5` 推荐 38 级、敌人 60 级；`ch23_2` 推荐 35、
敌人 60；`ch17_1` 推荐 25、敌人 45。是历次改表留下的没跟上的值。

### 29.2　改法

一次数据层脚本 `data/fix_chapters.py`，带自检，不过自检就不落盘：

1. **键序按 `req_stage` 链重排** —— 关卡表顺序 = 实际打的顺序。
2. **两段孤儿各自立章**，章数 31 → 33。郾城那段前移到小商桥之前
   （郾城七月初八、颍昌十四、小商桥十八、朱仙镇下旬，按史序），
   三处 `req_stage` 改接：`ch22_1←ch20_f2`、`ch21_1←ch22_boss`、`ch22_f1←ch21_f1`。
3. **6 关改名，对白一个字不动** —— 对白本来写的就是对的，是关名没跟上。
   `ch22_1` 的对白讲岳云十六岁打头阵，关名却叫「岳母刺字」；
   `ch22_boss` 的对白讲拐子马、铁浮屠、麻扎刀，关名却叫「岳飞大破金兵」。

   | 旧名 | 新名 |
   |---|---|
   | 云天彪初阵 | 云天彪独掌一军 |
   | 陈希真出山 | 刘慧娘的卦 |
   | 岳母刺字 | 岳云初阵 |
   | 枪挑小梁王 | 杨再兴请战 |
   | 岳飞大破金兵·Boss | 郾城破拐子马·Boss |

4. **两处撞车阵容改掉**：`ch19_2` 换成以刘慧娘为首，`ch23_3 岳飞班师`
   换成张宪、牛皋。首通奖与掉落不动，免得断了谁的获取路径。
5. **一章一名**，`story.chapters[n].title` 与 `ch_name` 统一，
   题词按内容迁移而不是按编号，补两段新的（雷将余威、郾城大捷）。
6. **`enemy_lv` 一律归到 `rec_lv[1]`。**
7. 秘藏关上的 `req_chapter`（55/63）是同一次改号的残留，引擎早就不读，清掉。

引擎里跟章号绑死的两处阈值跟着挪：`qCap` 的 `ch<=23` → `ch<=25`，
`enemy_star` 默认的 `ch>20` → `ch>21`。关卡 id、敌人阵容、掉落、存档键都没动，
老存档照读。

### 29.3　敌级归位带出来的连锁

`CFG.foeLv1` 写的是一周目敌方等级的实际区间，二周目靠它把等级整段平移。
归位之后实际区间从 3–60 收窄到 3–50，`foeLv1` 不跟着改，二周目的区间
就顶不满：50 级的关只映射到 57.5，band 上沿 60 永远用不上。

改成 `[3, 50]` 之后平移这一段本身变陡了 —— 中段每关都上浮 1–3 级，
二周目战斗场次从 158 场涨到 881 场。于是重标乘的那道微调：

| `lapFoeBy[2]` | 二周目场次（两局） |
|---|---|
| 1.00 | 139 / 139（毫无重试） |
| 1.06 | 177 / 272 |
| **1.12** | **234 / 310** |
| 1.18 | 292 / 991（一局停在 136） |

| `lapFoeMax` | 三周目场次（两局） |
|---|---|
| **1.18** | **139 / 196** |
| 1.24 | 两局都卡在 `ch30_f1` |
| 1.30 | 一局卡在 `ch22_boss` |

一周目是 158–234 场，取 1.12 / 1.18，三个周目在同一量级。

### 29.4　顺手查出来的：三周目的 mul 补偿

标定过程中发现三周目本来就打不过 —— **改动之前的版本连跑三局，三局全挂**，
只是之前只跑过一局，蒙对了。

原因是 `enemy_mul`。一周目它是拿来补等级差的：敌人才 20 级，靠系数硬撑难度，
个别关顶到 6。二周目起等级已经整段平移上去了，这道补偿却还在乘 ——
mul 6 的关在三周目是 6×1.18，玩家练满 70 级 ★7 也打不过。

加 `CFG.lapMulCap`，二周目起先削顶再乘微调：

| `lapMulCap` | 三周目（四局） |
|---|---|
| 不削 | 0 / 3 通 |
| 3 | 1 / 3 通 |
| **2.5** | **3 / 4 通** |

**剩下的一局挂在伤病上，不是系数能救的**：卡住时账上还有 2739 银两、
练级已到 70 级上限、伤员 24 人。赢不了 → 挂彩 → 阵容更弱 → 更赢不了。
这是伤病系统与后周目经济的账，单开一轮再算。

### 29.5　新的两个测试

- `test/laps.js` —— 多周目连打，不重置存档一路打到三周目，
  卡住时打印余银、练级上限、单级价、伤员数，好判断是难度问题还是经济问题。
  `--foemul=a,b`、`--mulcap=n` 可在页面里临时覆盖系数，用来扫参。
- `data/gen_stage_index.py` —— 按链序导出《关卡一览.md》。章号变了重跑一次，
  别手写。上一版就是手写的，所以它本身也没发现这些错。

### 29.6　验收

回归 23/23。通关模拟三局 139/139，196 / 158 / 158 场。
多周目连打四轮，三轮三个周目全通，一轮在三周目伤病打崩。

---

## 30　场景原画、尾声、装到桌面、图片外置

### 30.1　场景原画（8 张）
规范见 `场景原画生产规范.md`。按地貌出 8 张，覆盖 33 章，`story.chapters[n].scene` 由 `data/fix_scenes.py` 写入。
接在三处：章首卡（标题上方横幅，.55）、关卡详情页顶（120px，.30）、开篇字幕底（.18）。
接入时修了两处：`.chapcard>*` 的相对定位把「轻触继续」从底部拽到了正文里；章首图原来铺满竖屏，只剩中间一条且放大发糊，改成 3:2 横幅。

### 30.2　尾声
`VIEWS.epilogue`，版式同章首卡，配 `sc_shuipo`。头一回打通一周目时，结算页主按钮换成「尾声」；
看过记 `G.seenEpi`（进存档），之后主界面有「重看尾声」。

### 30.3　装到桌面 · 离线 · 禁缩放
- 图标：宣纸底 + 朱砂方印白字「義」+ 远山。`icon/`、`favicon.ico`、`site.webmanifest`，macOS 用 `icon/水浒群星录.icns`
- `src/pwa.js`：Chrome/Edge 一键装，Safari 给图文指引，Firefox 不弹；进主界面 45 秒后弹一次，点「以后再说」不再弹；主界面常驻「装到桌面」
- `sw.js`：代码网络优先，图片缓存优先；进来 8 秒后后台补齐全部图片，之后断网完整可玩。
  **首次接管不自动刷新**（clients.claim 也会触发 controllerchange，原写法会把新玩家刷一次）
- 禁缩放：viewport 锁 1.0，`touch-action: pan-x pan-y` 挡双击放大，iOS 另拦 gesture 事件，桌面拦 Ctrl+滚轮

### 30.4　图片外置（放弃单文件）
内嵌时 gzip 后 4.33MB，图片占 4.18MB、代码加数据 144KB，且要整页下完才启动。
改为 html 只写 `相对路径?v=内容哈希`，`<img loading="lazy">` 按需取。

| | 内嵌 | 外置 |
|---|---|---|
| html（gzip） | 4331KB | 151KB |
| 首屏出画面（10Mbps 实测） | 5.3s | 0.6s |
| 每次上线后重下 | 4.3MB | 约 150KB |

本地双击 html 仍可玩，但要连 `assets/` 一起拷。改图后重跑 `build.py`，哈希变了缓存自动换新。

---

## 31　V10.1：导航、详情切换、死属性、碎片、武将只能靠抽

### 31.1　导航
四列两行：聚义 · 关隘 · 布阵 · 群将 / 酒肆 · 行囊 · 羁绊 · 图鉴。
「聚义」就是主界面，原来导航里没有它，进了哪一页都回不了首页。子页面（关卡详情、招贤结果、武将详情）亮它所属的那一格。

### 31.2　武将详情左右切换
- 从哪一页点进来就在哪一串人里切：布阵 → 出战九人；群将 → 当前排序筛选；图鉴 → 已收的人按图鉴顺序；招贤结果 → 这一抽的人。别处进来，在阵的按阵容切，不在阵的只有自己。
- 顶上一行「‹ 上一个　3 / 9　下一个 ›」；手机横滑（touch 事件，竖滑照常翻页），电脑左右方向键。
- 「返回」回到进来的那一页（原来一律回群将）。
- 在详情里下阵，这一串不马上把人踢掉，免得一按就跳到别人。

### 31.3　属性：五项，每项都有用处
| | 管什么 | 详情页写法 |
|---|---|---|
| 武 | 伤害 | 伤害 |
| 防 | 减伤 | 减伤 |
| 智 | 技能威力：攻击技能伤害、护盾量 × (1 + 0.4 × 智/(智+武))；治疗量本来就按智；混乱命中看双方智的比 | 技能威力 +x% |
| 捷 | 出手先后；暴击率 = 8% + min(10%, 捷/1800) + 专属暴击 | 先手 · 暴击 x% |
| 血 | | |

魅删掉：武将身上的删；17 个加减魅的技能改成加减捷；装备上的魅坐骑折捷、别的折智。
暴击率与技能威力是 `critRate()` / `skillPow()` 两个函数，战斗和详情页共用。

### 31.4　装备死字段与专属（`data/fix_equip_stats.py`，可重跑）
- `sk`（技能倍率，79 件）引擎从没读过 → 折成智（1.10 ≈ 智 +20）；`lead`（统率，6 件）→ 折成智；`other_bonus` 删
- 19 件「专属神兵」原来缺槽位缺品阶，被当成「良」品宝物白送（第一章就能捡到武 +60）→ 补成天罡兵器、绑定本人，描述里的加成写进 `owner_bonus`
- 描述写着「某某专属」的按描述绑人（丈八蛇矛原来绑的是卢俊义）
- 一人一件专属：75 人各留一件；同名、无面板的删 26 件（写进 `equip_remap`，读档时换成留下的那件）；另外 18 件降成普通装备进掉落池
- 专属加成按件上写的来（武/防/智/捷/血/暴击/全属性），原来只认武力；非本人穿只拿面板
- 专属只从 Boss 关掉：打赢 Boss（含速战）3% 摇一件，先挑这一关敌将的，再挑手上的人还没拿到的
- 详情页、行囊、选装备弹层把装备属性写全（原来弹层不显示防御百分比，行囊只有名字）

### 31.5　碎片
- 升星只剩两样：本人碎片 + 兵符。三种无主碎片取消，关卡掉的改掉兵符；读档时按 1/3/5 片折成兵符
- 先扣本人碎片，不够用兵符补。按钮写「升星 · 片 3/8+符 5」
- 满星之后，手上的和以后抽到的本人碎片 2 片折 1 枚兵符（`CFG.fragMelt`），零头留着
- `Grow.starNeed()` 一处算账，升星按钮、群将「可升星」、首页提示都读它（原来三处各算各的，说法对不上）
- 行囊的碎片一行写「下一星要 8，差的 5 片用兵符补」，点进那个人的详情

### 31.6　武将只能靠抽
- 关卡掉落里的武将（186 处）、首通送的武将（139 处）全部拿掉，结算那头也不再认 hero 掉落
- 开局银两 500 → 5000，够一次十连；首页引导先去酒肆

### 31.7　难度重调
原难度表是在「关卡一路送人」和「十九件神兵白送」这两个前提下调出来的。两样都拿掉后照原表打，一周目要上千场、末章撞满级墙。
- 一周目按章折扣 `CFG.foeEase`：`[[1,.65],[8,.72],[20,.8],[27,.8],[33,.72]]`，乘在关卡强度系数上，只管一周目
- 二、三周目微调 `lapFoeBy[2]` 1.12 → 1.00，`lapFoeMax` 1.18 → 1.06

模拟器也跟着改：`playthrough.js` 加 `--roster= --pull= --nomed --ease=`，`laps.js` 先抽够 20 人、有钱就十连。
老的模拟策略是「治伤钱压着不动、只在第一次打这关时才抽」，人只能靠抽之后它会把钱全攒着不抽，卡死在第十五章，不代表真人。

### 31.8　验收
- 回归 22/23（「伤势记账与休养」V10.0 就挂着，没动）
- 一周目四局全通：272 / 367 / 424 / 370 场（V10.0 是 158–196 场）。卡点在第三、五、八章和末章
- 连打三周目三轮：二周目 139–406 场全过；三周目三轮过两轮
- 数值对照：同一份 V10.0 存档读进 V10.1，第 3、8、15 章各关胜率与 V10.0 相同（差在 ±5% 内）——属性改动本身不改变强弱，难度上升来自拿掉白送的人和神兵
- 存档迁移：无主碎片折符、满星碎片折符、删掉的装备换成留下的那件（槽位不对的卸回行囊）、魅字段删除，均实测
